@/home/chlience/.codex/RTK.md
@/home/chlience/.codex/first_principles.md
@/home/chlience/.codex/git_commit.md

## 写作语气

- 中文输出避免使用先否定前项、再强调后项的对照句式。
- 避免通过否定别人或否定前项来强调观点。
- 优先使用直接、顺承、解释型表达。例如，优先写“它由一条多阶段流水线组成”。
- 避免空泛、拟人化或用力过猛的修辞词，优先使用具体动作、状态、变量和结果。
- 中文技术术语先做目标领域词汇化检验：判断依据取自中文论文、文档和技术社区的实际使用状态；单个偶发用例只作弱证据，缺少充分证据时按未归一化处理。
- 未归一化的词按实际功能改写：实验分组写为“实验组/对照组/实验设置/系统配置/方法变体”，流程组成写为“阶段/环节/路径/分支”，结果地位写为“主要结果/汇总结论”，剩余能力或空间写为“余量/可优化空间”。
- 术语首次出现且功能性改写可能丢失与原文对应关系时，在中文译名或改写后以括号附英文原词。
- 直译词已是目标领域既定术语时直接保留；逐字引用中文原文时保留原词，逐字引用英文原文时保留英文。
- LLM、机器学习与系统论文中的 `experimental/study arm` 通常写为“实验组”“对照组”“实验设置”“系统配置”或“方法变体”；“臂”保留于多臂老虎机、临床试验既定术语或原文引用。

## 论文存档工作流

- 本目录只服务论文阅读、分析、作者关系跟踪、研究主线和 Markdown 存档。
- 单篇请求先选择交付模式：`analysis-only` 响应“分析、总结、解释”等只读请求；`archive-core` 响应“归档、建档、保存、commit、push”等发布请求。`enrichment` 是叠加在任一交付模式上的范围修饰项，只执行用户明确要求或会改变核心结论的审稿、代码、复现、作者档案、公开账号和跨论文维护；它本身不授予仓库写入权限。
- `analysis-only` 不创建或修改仓库文件。用户随后要求 commit 或 push 时，先完成 `archive-core`，再执行对应 Git 操作。
- `archive-core` 使用 `content/utility/paper-note-template.md` 的 v2.1 结构，将论文沉淀为 `content/papers/<slug>.md`，在 `content/utility/papers-index.md` 保留唯一一行，并在 `data/paper-tags.json` 分配一个主标签和最多三个辅助标签。
- `analysis-only` 与 `archive-core` 均先阅读规范来源和回答用户问题所需的正文或附录。`analysis-only` 只采用规范来源明确显示的审稿状态；`archive-core` 最多增加一次标题或材料 ID 匹配，未找到即记录 `not-found`。公开审稿全文、代码历史和外部身份来源只在用户明确要求、来源冲突或缺少它们会改变核心结论时进入范围。
- 论文核心信号与 `一句话结论` 使用可独立成立的自然语言句子，说明研究对象、区别性机制、主要结果和会改变真值的边界；不使用公式、TeX 定界符或 Theme 式关键词列表。
- 每篇论文包含 `Source`、`作者与关系`、`一句话结论`、`论文脉络`、`关键实验/定理`、`局限`、`跨论文关系`；条件模块按 `Analysis modules` 启用。
- `论文脉络` 是单篇笔记最重要的分析正文：先给出首要贡献与辅助贡献的全景，再按问题形成、核心假设、方法执行顺序、各环节作用、直接证据和成立边界展开；抽象机制在有助于理解时加入可与原定义逐项对应的具体例子。
- 方法包含多个关键阶段、角色、模型变体、训练分支或论证依赖时，固定以 `### 5. 贡献全景与方法总览` 开始。第 5 节独立给出首要贡献、辅助贡献和端到端执行链或论证链，说明起点、关键阶段及其顺序、阶段间传递对象、每个阶段的作用和最终输出、训练信号或结论；从 `### 6.` 开始至少展开首要机制，紧密耦合的阶段可以合并解释，只有省略后会妨碍复述机制、证据或成立边界的阶段才单独成节。
- 同一名称指向不同模型、策略、数据版本、状态或层级对象时，第 5 节先明确各对象的角色、所属阶段和更新关系，后文保持称谓一致。阶段详解与总览保持顺序、对象和接口一致；后文增加关键角色、分支、传递对象或训练信号时同步回写总览。
- 方法只有一个实质阶段时，使用 `### 5. 方法总览与完整机制`，先说明首要贡献及其与该阶段的关系，再概括输入、核心操作、输出或结论和设计理由，随后在同节解释机制、直接证据与成立边界。
- 本契约中的“方法”统称论文的可复述工作链，覆盖算法执行流程、系统数据路径、理论论证链、综述分析框架和文档行为链。
- 方法详细度以两层复述为验收标准：只读第 5 节可以复述首要贡献、完整链条、关键对象及其角色差异，以及最终输出、训练信号或论证结论；读完必要的阶段详解后可以进一步复述首要机制的输入、核心操作、传递对象、输出或训练信号、设计理由、直接证据和失败边界。方法篇幅不设统一字数，由解释闭环决定，不按微阶段逐节复制。
- `internal/paper-workflow-method-overview-baseline.json` 只冻结新契约启用时尚未迁移的 v2.1 笔记及其 `Updated-At`。验证器固定初始 `slug`—`Updated-At` 上限，活动记录只能随迁移缩减。基线时间未变化时缺口进入提示（advisory）；笔记发生实质更新时先移除对应基线记录并补齐方法总览，新增笔记不能进入该基线。
- 关键公式前说明它解决的局部问题，公式后解释变量角色、信号方向、实现后果和适用条件。来源未披露的数据规模、超参数、消融或实现细节在相邻位置明确标注，不用概括性语言补足证据缺口。
- 论文头部声明 `Review-Status`，只使用 `pending` 和 `approved`。新增或未经用户确认的论文使用 `pending`，且不记录 `Reviewed-At`；用户确认后改为 `approved` 并记录分钟精度的 `Reviewed-At`。后续修订只更新 `Updated-At`，保留原有 `Review-Status` 和 `Reviewed-At`。
- `主要启发` 是单篇笔记中稳定认识更新的唯一归档位置。首要启发与辅助启发使用同一筛选条件：候选需要具备可归因的认识变化、独立的作用机制、区分性证据和迁移预测；没有独立认识更新时省略本节，不预设数量上限或目标数量。
- 主要启发先在内部按“可归因的原有判断—认识触发点—关键转换—作用机制—区分性证据—新判断—抽象关系—迁移检查—来源与边界”构建。迁移采用“抽象关系—目标实例化—可证伪预测”，不预设论文类型特定的角色。
- 公开正文以可独立成立的新判断命名，用二至三个自然段交代论文专属证据与机制、抽象后的迁移预测和成立边界。方法摘要、结果复述、通用提醒、未披露项、复验清单、宽泛类比和无区分性证据的机制猜测分别归入其它功能章节。
- 后续交流形成的长期判断按语义归入当前笔记的 `论文脉络`、`关键实验/定理`、`局限`、`跨论文关系` 或 `主要启发`，不再新增公开的 `本地讨论补充`。旧笔记只在新证据纠正事实错误、改变一句话结论真值，或用户明确要求维护时回写并迁移存量章节；核心信号变化时更新论文索引。
- `archive-core` 只保留论文作者块、论文时机构、本地重名检查和已有 profile 链接，不新建或扩充作者 profile。作者 `enrichment` 先限定为“指定作者 × 指定字段”；只有用户要求完整作者关系或 profile 完善时，才扩展到作者顺序前两位、明确共同一作和明确通讯作者。普通作者只复用已有 profile，用户明确要求时例外。
- 作者 `enrichment` 包含代表论文或 profile 完善时，才将已核验个人主页明确选中或重点展示的论文写入 `data/authors.json -> representativePapers`；它们与本站已有单篇论文笔记分开记录。
- `archive-core` 保留直接支撑或限制一句话结论所需的关键结果，数量由独立证据和结论边界决定；同一证据只在一个主要位置完整展开。
- 用户问题已经回答、核心主张已有一手证据、结论边界稳定且新增来源不再改变判断时，停止检索。
- 删除论文时同步删除索引行和标签，修订失效关系与主线引用，并审计 `data/authors.json`；无剩余论文关联的 profile 同提交删除。

## Model Summary 结构

- 模型技术报告、模型发布仓库等材料的 `Model Summary` 优先采用紧凑的 `Field | Value` 两列表格，字段顺序参考 K3 GitHub：

  | Field | Value |
  | --- | --- |
  | Architecture | ... |
  | Total Parameters | ... |
  | Activated Parameters | ... |
  | Number of Layers | ... |
  | Number of Dense Layers | ... |
  | Attention-Layer Composition | ... |
  | Attention Hidden Dimension | ... |
  | Number of Attention Heads | ... |
  | Latent MoE Dimension | ... |
  | MoE Hidden Dimension (per Expert) | ... |
  | Number of Experts | ... |
  | Selected Experts per Token | ... |
  | Number of Shared Experts | ... |
  | Vocabulary Size | ... |
  | Context Length | ... |
  | Attention Mechanism | ... |
  | Activation Function | ... |
  | Vision Encoder | ... |
  | Parameters of Vision Encoder | ... |
  | Quantization | ... |
  | Modality | ... |

- 按具体模型保留适用字段；架构特有且影响理解或部署的配置可在相邻位置补充。官方材料未披露的关键字段写为“未披露”，本地推导值明确标注统计口径和证据来源。
- 总参数与激活参数、模型隐藏维度与注意力隐藏维度、MoE 潜空间与单专家隐藏维度、专家总数与每 token 选中专家数及共享专家数分别记录。量化项同时说明权重、激活值和量化感知训练状态；模态单列。

## 研究主线工作流

- 用户明确提出某个方向的总结、综述、进展或时间线请求时，直接建立一条正式主线；一次请求对应一条主线，不自动合并、拆分或改名。
- 主线保存在 `content/mainlines/<slug>.md`，公开路径为 `/mainlines/<slug>/`，使用 `content/utility/research-synthesis-workflow.md` 与 `content/utility/research-mainline-template.md` 的 `synthesis-v1` 独立结构。
- 主线标题和 slug 保持日期无关；检索时间窗、截止分钟、版本变化和时间阶段写入文章内部。
- 主线文章是问题、边界、分类、材料、证据强度和当前判断的唯一事实来源。每条主线使用自己的主题专属分类框架，不维护全局 facet。
- 主线身份无需审阅；文章内容使用 `pending` 和 `approved`，后续修订保留原有审阅状态与审阅时间。普通论文新增不会自动更新主线，只有用户明确要求时更新。
- 主线不进入论文索引、论文标签、论文数量或论文审阅筛选；进入全局检索。本地 `/papers/<slug>/` 链接声明成员关系，同一论文可进入多条主线，论文页回链由构建器生成。
- 主线可直接纳入外部原始材料。单篇笔记保留局部证据，主线保存规范化跨材料比较与逐结论证据强度。
- 主线固定章节为 `Source`、`综合判断`、`核心问题与边界`、`分类框架`、`演进脉络`、`跨材料比较`、`证据强度`、`当前判断`、`开放问题`、`局限`、`更新记录`。

## 通用发布规则

- v2.1 单篇笔记与 synthesis-v1 主线均在 `Source` 声明 `Key figure decision: include|omit`；`include` 至少嵌入一张带 `Image Source` 的本地图片，`omit` 必须填写 `Key figure rationale`。
- 站点链接使用 `/papers/<slug>/`、`/mainlines/<slug>/`、`/topics/#tag-<id>`、`/workflow/`、`/synthesis-workflow/`、`/template/`、`/mainline-template/` 和 `/archive/`。
- 安全或双用途材料保留机制、风险、评测和防御启发，避免沉淀可直接滥用的操作细节。
- 提交前由主执行者完成一次合并科学自审，检查核心贡献对齐、机制或论证链、直接证据定位、最窄结论边界、局限与成立条件。核心证据冲突、结论高风险或用户明确要求独立复核时再启用独立审查者；只有修改作者档案或其它增强元数据时才增加元数据审计。自动检查不能替代人工判断。
- 修改强制工作流要求时，同步检查本文件、SOP、模板、验证器、测试和至少一个新契约 fixture 或真实 canary。
- 完整改动验证后创建本地 commit；`push` 只在用户明确要求时执行。

## 高危操作确认

- 执行递归删除、整目录删除、大范围清理、覆盖移动、不可逆 Git 操作或其它可能造成数据丢失的命令前，必须向用户二次确认。
- 用户确认前只做只读检查和风险说明；确认信息列出绝对路径、操作类型和主要风险。
