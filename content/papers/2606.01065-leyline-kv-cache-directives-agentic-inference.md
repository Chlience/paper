# Leyline: KV Cache Directives for Agentic Inference 论文笔记

First-Archived-At: 2026-07-14 10:56
Updated-At: 2026-07-14 11:13

## Source

- Workflow version: v2
- Material type: research-paper
- Canonical source: https://arxiv.org/abs/2606.01065
- Title: Leyline: KV Cache Directives for Agentic Inference
- Authors: Bole Ma, [Jan Eitzinger](/authors/jan-eitzinger/), [Harald Köstler](/authors/harald-koestler/)
- Responsible organization: Erlangen National High Performance Computing Center（NHR@FAU）
- arXiv: https://arxiv.org/abs/2606.01065
- HTML: https://arxiv.org/html/2606.01065
- PDF: https://arxiv.org/pdf/2606.01065
- TeX source: https://export.arxiv.org/e-print/2606.01065
- Code/Project: arXiv 元数据未提供代码链接；Appendix O 写明将在去匿名后发布，Appendix W 又称 patch 已按 Apache-2.0 发布，当前未能从官方来源定位公开仓库
- OpenReview / Review page: 未发现可可靠匹配的公开投稿或审稿页（核验于 2026-07-14）
- Submitted: 2026-05-31 07:13:15 UTC
- Published / updated: 2026-05-31（arXiv v1）
- Current version read: arXiv v1；HTML；PDF；TeX source
- Version / revision read: arXiv:2606.01065v1
- Accessed: 2026-07-14
- Subjects: cs.DC（primary）；cs.AI；cs.LG

## 作者与关系

- Bole Ma: Erlangen National High Performance Computing Center（NHR@FAU）。
- [Jan Eitzinger](/authors/jan-eitzinger/): Erlangen National High Performance Computing Center（NHR@FAU）。
- [Harald Köstler](/authors/harald-koestler/): Erlangen National High Performance Computing Center（NHR@FAU）。

三位作者位于同一 NHR@FAU 团队。机构页面记录 Bole Ma 为 Software & Tools 方向博士生，Jan Eitzinger 为 Software & Tools 负责人，Harald Köstler 为 AI Support 负责人。三人此前共同完成 Irminsul，先处理 agent context 中“相同内容移动位置后的复用”，Leyline 延续同一 $\delta$-rotation 基础，进一步处理 policy 主动删除或替换历史 span 的 mutation。当前档案未发现三人与 Grape、SPORK 作者重叠。

## 一句话结论

Leyline 在 agent policy 与 KV Cache 之间增加语义编辑通道：policy 用 `(span, replacement, mode)` directive 声明需要修改的历史范围和语义保证，serving 层保留未修改前缀、重算 replacement，并用 RoPE $\delta$-rotation 重标 MLA 后缀 KV 的位置，从而在 `AMORTIZE` 模式下减少上下文编辑后的重复 prefill，在 `FORGET` 模式下回退到 prefix-trimmed re-prefill 实现真正遗忘。

本地评价：论文最重要的贡献是把 cache mutation 提升为 agent-facing contract，并明确区分位置摊销与语义遗忘。机制实验给出 radix hit rate `49.6% → 60.8%`、峰值 p50 延迟下降 241 ms 的证据；policy 实验的 solve rate `31.2% → 45.5%` 来自标准 re-prefill 下的截断规则，只支持“上下文策略有价值”，无法归因给 splice kernel。`AMORTIZE` 会保留原 span 对后续 KV 的历史影响，MLA 之外的高效正确实现、联合 policy×kernel 实验和公开 artifact 仍是主要缺口。

## 阅读目标与判断边界

本笔记关注：

1. agent 主动编辑历史时，传统 prefix cache 为什么会丢失 edit point 后的大量已算 KV。
2. directive、`AMORTIZE` / `FORGET` 和 $\delta$-rotation 分别提供什么语义与系统保证。
3. 机制实验、policy 实验和真实轨迹分别支持哪些结论，以及三组证据能否合并归因。

判断边界：

- `AMORTIZE` 保证后缀位置校正并保留原上下文影响；它不承诺与“对替换后 prompt 完整 re-prefill”相同。
- `FORGET` 走 prefix-trimmed re-prefill，提供删除、纠错或合规场景所需的语义边界，同时保留重算成本。
- 高效 kernel 的主要实证对象是 MLA；GQA / MHA 只有代数与小规模 live-model 检查，边界重算仍待实现。
- 论文将机制与 policy 分两条腿验证，没有给出 splice kernel 加速截断 policy 的联合消融。
- 论文描述了约 200 LOC SGLang RadixCache patch，但公开仓库当前无法从官方来源定位。

证据写法：

- 论文事实：arXiv v1 正文、附录、表格和 TeX source 直接给出的接口、公式、配置与数字。
- 作者主张：引言、讨论与结论对 cache-edit channel、可编程 memory 和架构扩展的定位。
- 本地分析：对语义契约、组合归因、跨论文接口和部署一致性的重建。
- 关键判断附章节、图表、附录和 PDF 页码。

## 论文脉络

### 1. 研究问题、背景和价值

传统 LLM serving 把 prompt 视为单调增长序列。token 完成 prefill 后，内容和绝对位置通常保持不变，因此 radix / prefix cache 可以从第一个 token 开始匹配连续前缀，后续请求只计算新增 suffix。

长程 agent 会主动重写历史：失败工具输出被替换，旧日志被截断成 stub，过期调查块被摘要，retry 可能丢弃一段轨迹。设原始 prompt 为

$$
P=A\Vert B\Vert C,
$$

policy 将中间 span $B$ 替换为更短的 $R$，新 prompt 为

$$
P'=A\Vert R\Vert C.
$$

radix cache 只能稳定命中 $A$。即使 $C$ 的 token 内容保持不变，它的绝对位置已经改变，原有 RoPE key 也锚定在旧位置；常规 harness 会从 edit point 开始重新 prefill $R\Vert C$。编辑越靠前、轨迹越长，重复计算越多。

论文进一步区分两个 cache 问题：自然发生的相同内容位置迁移属于 position-invariant reuse；policy 明确要求删除或替换 span 属于 policy-driven mutation。Leyline 聚焦后者。证据定位：Section 1，Figure 1，PDF pp.1-2；Table 1，PDF p.3。

### 2. 已有解决方案与不足

- Prefix / radix cache 复用连续前缀；任意中段编辑会截断后续命中。
- H2O、SnapKV、StreamingLLM 等 kernel-side eviction 根据系统或注意力信号自主选择 token，缺少 agent policy 可审计的 span-level mutation 接口。
- LMCache 的 Pin、Clear、Move、Compress、Lookup 操作物理 KV block 和存储层级；它没有定义语义 span 编辑后的位置正确性。
- CacheBlend、EPIC 通过选择性重算支持 MHA / GQA 的非前缀复用；Irminsul 用 content hash、CDC chunk 和 $\delta$-rotation 处理 MLA 中自然位置迁移。
- 供应商 context editing API 通常在消息层修改历史并执行 re-prefill，能够获得替换后 prompt 的语义，却会丢失 edit point 后的缓存工作。

Leyline 将 policy intent、语义模式与架构特定 kernel 分层：上层声明修改范围和需要的保证，下层选择 in-place splice 或 re-prefill。证据定位：Section 2，Table 1，PDF p.3。

### 3. 作者可能的思考路径

以下为本地分析的受约束重建：

1. 先观察 agent 每轮重新渲染 history 的路径，发现一次中段 message edit 会让 radix hit 在 edit point 之后归零。
2. 将“相同内容自然移动”和“policy 主动修改”分开，确认后者需要显式 agent→serving 控制通道。
3. 复用 Irminsul 在 MLA 上验证过的 RoPE 位置校正，把 content-hash 触发改成 policy directive 触发。
4. 发现原 span 已经进入后续 token 的 hidden state；单纯旋转后缀 key 只能校正位置，无法消除原内容的历史影响，因此把接口拆成 `AMORTIZE` 与 `FORGET` 两种 mode。
5. 用机制 microbenchmark 验证缓存和延迟，再用简单截断 policy 验证 span signal 是否能产生 agent-level 价值，形成两条独立证据链。

### 4. 核心假设或切入点

Leyline 的 splice 路径依赖四个条件：

1. **显式编辑边界**：policy 能把消息级决策稳定映射为 rendered prompt 中的 token span。
2. **RoPE 可校正性**：position shift 可通过旋转群闭包在缓存 key 上做 closed-form correction。
3. **可接受的持久影响**：选择 `AMORTIZE` 时，部署方接受原 span 已经写入 downstream hidden state 这一事实。
4. **存在重算 headroom**：vanilla radix cache 在 edit 后仍有较长、内容不变的 suffix；已有命中很高时，splice 可节省的工作很少。

前两项决定机制能否执行，第三项决定语义是否适用，第四项决定性能收益。

### 5. 方法 / 系统框架

#### 5.1 Directive：把 policy intent 显式化

directive 定义为四元组：

$$
D=(s_{\mathrm{start}},s_{\mathrm{end}},R,m),
$$

其中 $[s_{\mathrm{start}},s_{\mathrm{end}})$ 是原 rendered prompt 的 token 区间，$R$ 是 replacement tokens，$m\in\{\texttt{AMORTIZE},\texttt{FORGET}\}$ 是语义模式。同一 turn 可以提交多个互不重叠的 directives；kernel 从左到右处理，重叠 span 在 apply-time 拒绝。证据定位：Section 3.1，PDF pp.3-4；Appendix C。

policy SDK 暴露 `Policy.transform(messages, turn_idx) -> messages`。Leyline 对比上一 turn，计算 message diff 并生成 token directive。信号可以来自 turn counter、手写规则、content matcher 或 learned detector；kernel 不检查 policy 的理由。证据定位：Section 3.4，PDF p.5。

#### 5.2 两种 mode 对应两种语义

设 replacement 相对原 span 的长度变化为

$$
\Delta=|R|-(s_{\mathrm{end}}-s_{\mathrm{start}}).
$$

`AMORTIZE` 校正 edit 后 suffix 的位置，并保留 downstream `K_nope` 与 `V`。这些状态在原 span 存在时已经计算，因此输出继续携带原内容的历史影响。新的 stub 会进入显式 token 序列，缓存状态同时保留已传播到 downstream state 的旧信息。这个 mode 适用于低成本缩短展示历史、减少重复 prefill，并要求调用方接受 positional replay-equivalence。

`FORGET` 从保留前缀后重新 prefill replacement 和 suffix。敏感信息删除、错误工具输出修正、retention-mandated deletion 等场景需要这条路径。directive 层的增量价值是让语义意图可声明、可审计；kernel 性能增量集中在 `AMORTIZE`。证据定位：Section 3.1，PDF p.4。

#### 5.3 MLA 上的 $\delta$-rotation

MLA 将缓存 key 分成 position-free 部分 $K_{\mathrm{nope}}$ 和使用 RoPE 的位置部分 $K_{\mathrm{pe}}$。对原位置 $i\ge s_{\mathrm{end}}$ 的 suffix token，splice 后逻辑位置变成 $i+\Delta$。Leyline 更新：

$$
K^{\mathrm{new}}_{\mathrm{pe}}[i+\Delta]
=
\mathcal R(\Delta)K^{\mathrm{old}}_{\mathrm{pe}}[i].
$$

因为

$$
\mathcal R(\Delta)\mathcal R(i)=\mathcal R(i+\Delta),
$$

旋转后的 $K_{\mathrm{pe}}$ 与在新位置生成的位置分量一致。MLA 只需旋转 64 维位置 band；$K_{\mathrm{nope}}$ 与 $V$ 保持原值。多次 edit 的旋转按 $\mathcal R(\Delta_1)\mathcal R(\Delta_2)=\mathcal R(\Delta_1+\Delta_2)$ 组合。证据定位：Section 3.2，Equation 1，PDF p.4。

#### 5.4 Serving 集成

一次 directive 被分成三段执行：

1. edit 之前的 prefix 继续由 radix cache 命中。
2. 被替换 span 的 slots 被释放，replacement $R$ fresh prefill 到新 slots。
3. edit 之后的 suffix slots 执行 $K_{\mathrm{pe}}$ rotation 和逻辑位置重索引。

SGLang Role B 路径随后把修正后的 token-prefix→slot 映射插入 radix trie，再走原生 `match_prefix`，让未来请求继续发现这些 KV。论文描述的实现包含约 200 LOC RadixCache patch 和 content-hash side index；当前公开 artifact 尚无法定位。证据定位：Section 3.3，Figure 2，PDF pp.4-5；Appendix R。

#### 5.5 数值与架构边界

rotation 计算默认使用 fp32，结果仍写入 bf16 KV pool。论文测得 bf16 storage 带来约 1%-3% per-K-entry error floor；50-step replay 中 48/50 个 first token 与 full-prefill reference 相同。fp32 rotation 能降低旋转计算误差，无法消除 bf16 存储误差。

MLA 把 position 集中在较小 $K_{\mathrm{pe}}$ band，提供最干净的高效路径。GQA / MHA 需要旋转 full-width K，模型对已含原内容的 downstream state 更敏感；作者提出加入 chunk-boundary recomputation。ALiBi、NoPE 与 learned absolute position 仍未覆盖。证据定位：Sections 4、6，PDF pp.6-7、9-10；Appendices H、Q。

### 6. 结论链条

1. Agent history 的中段编辑会让 prefix cache 在 edit point 后丢失命中，即使大量 suffix token 内容保持不变。
2. Policy 比 storage layer 更早知道哪些语义 span 应被替换，因此需要 agent-facing cache-edit channel。
3. Directive 将 span、replacement 和语义 mode 绑定；`AMORTIZE` 与 `FORGET` 明确区分位置摊销和真正遗忘。
4. MLA 的 RoPE 位置 band 可以用 $\delta$-rotation 重标，保留未改 prefix 和已算 suffix KV。
5. 机制实验支持 cache-hit 与 re-prefill 延迟收益，policy 实验支持简单 context truncation 对部分长任务有帮助；两条证据尚未合并为联合系统结论。

## 关键实验/定理

### 结果 1：splice 在受控 message-edit workload 中恢复 suffix cache hit

- 设置：DeepSeek-V2-Lite，单张 H100，greedy decode；16 sessions，每个 session 6 turns；system prompt 约 4096 tokens，每 turn 约 2048 tokens，末轮约 17K prompt；并发 $C\in\{1,4,8,16\}$。
- Baseline：关闭 radix cache；标准 SGLang radix；Leyline splice。
- 指标：replay cache-hit ratio；p50 end-to-end latency；rotation kernel time。
- 结果：标准 radix 为 49.6%，splice 为 60.8%，所有并发下均增加 11.2 pp。p50 延迟分别为：$C=1$ 时 4087→4036 ms，$C=4$ 时 4729→4644 ms，$C=8$ 时 5533→5292 ms，$C=16$ 时 7166→7211 ms。峰值下降 241 ms（4.4%），累计 kernel time 不超过 2.9 ms/run。
- 证据定位：Appendix B，Table 3，PDF pp.15-18。
- 对照是否可比：三臂使用同一 SGLang scheduler 与 workload，mechanism 对照较强；prompt 是合成多主题 message edit，跨请求 discovery 和真实 policy 分布仍有限。
- 支持的最窄结论：在 17K、内容不变 suffix 较长的 MLA message-edit workload 上，in-place splice 能稳定恢复 11.2 pp cache hit，并在 prefill contention 较高的 $C=8$ 单元降低 p50。
- 解读：收益随并发非单调。$C=16$ 已进入 decode-dominated 区域，节省的 prefill 没有继续移动 p50；cache-hit 增加也不自动转化为端到端收益。

### 结果 2：截断旧工具输出改善少数长程 debug-gym 任务

- 设置：JoyAI-LLM Flash，H200 TP=4，SGLang；8 个 debug-gym `mini_nightmare` tasks，4 seeds，最多 50 agent steps。
- Baseline：`keep_all`，32 trials。
- Treatment：`truncate_older_than(n=2,max_chars=200)`，33 trials；旧 tool message 被替换为保留首尾的 200-character stub。
- 指标：hidden-test solve rate。
- 结果：10/32（31.2%）→15/33（45.5%），增加 14.3 pp。主要增量来自 `shopping_cart` 0/4→3/4 和 `sum_tree` 1/4→2/4。
- 证据定位：Section 5，Table 2，PDF pp.7、9；Appendix A，PDF p.14。
- 对照是否可比：两组都走 standard re-prefill，唯一变化是 message policy；样本少、只测一个 threshold，未报告置信区间。
- 支持的最窄结论：对这组 debug-gym 任务，保留最近两轮并截断更早 bash 输出的规则提高 aggregate solve rate，收益集中在两项较长任务。
- 解读：这 14.3 pp 属于 policy 证据。两组均未使用 splice，因此无法支持 Leyline kernel 改善 solve rate 或降低该 policy 的端到端延迟。

### 结果 3：真实轨迹显示收益取决于 radix headroom，数值一致性受 bf16 floor 限制

- 设置：50-step `shopping_cart` debug-gym trace，prompt 从 334 增长到约 28K，$C=1$。
- Baseline：标准 SGLang RadixCache。
- 指标：sum end-to-end time、cache hit、first-token agreement。
- 结果：full configuration 从 52.65 s / 27.6% hit 变为 49.85 s / 27.8% hit，wall time 下降 5.3%，first-token agreement 为 48/50。作者分解约 95% timing win 来自 fp32 rotation mitigation，Role B L2 在单轨迹上 timing-neutral。另一条 radix hit 达 91.6% 的轨迹中 splice 慢 0.6%，20/20 输出相同。
- 证据定位：Section 5，PDF pp.7-8；Appendix T，PDF p.28；Appendix Q。
- 对照是否可比：同 trace 对照直接，但两条 trace 的 radix headroom 差异很大；fp32 mitigation 与 splice discovery 同时进入实现。
- 支持的最窄结论：该低-hit 轨迹支持 full configuration 降低少量 wall time，其中 cache hit 只变化 0.2 pp，性能证据更直接指向 fp32 rotation mitigation，无法归因给 Role B cache reuse；已有命中高时收益趋近零。bf16 KV storage 仍可能造成低 logit-margin 下的 token 分歧。
- 解读：Leyline 优化的是重复 prefill compute / writes，Role B trie pin 不降低 peak allocator watermark。它更接近 cache reuse primitive，而非 KV 容量压缩方法。

### 实验设置与 baseline 审计

| 维度 | 记录 |
| --- | --- |
| 评测协议 | 机制 leg 使用合成 message-edit replay；policy leg 使用 8 个 debug-gym tasks×4 seeds；另有 12-step 四 MLA 模型 replay、random edit stress 和 50-step trace。 |
| 统计报告 | 报 cache hit、p50/p99、solve fraction 和一致性；policy 结果无 CI 或显著性检验，单 threshold。 |
| Baseline 是否 tuned | SGLang radix 使用默认路径；policy baseline 为 keep-all。未报告 policy threshold sweep。 |
| Baseline 是否 compute-matched | 机制三臂同模型、同 workload；cache 行为不同正是实验变量。policy 两臂都走 re-prefill。 |
| Baseline 是否 implementation-matched | 机制 leg 位于同一 SGLang scheduler，匹配度高；cross-architecture replay 主要是离线 kernel。 |
| Baseline 是否覆盖强替代方案 | 讨论 CacheBlend / EPIC / Irminsul / LMCache，未在同 workload 直接跑强替代实现。 |
| Baseline 是否存在弱化风险 | 合成 workload 人为保证 edit 后存在长相同 suffix；真实 trace 显示高 radix hit 会消除 headroom。 |
| 结论边界 | +11.2 pp / 241 ms 属于 mechanism；+14.3 pp 属于 policy；两者不可相乘或合并归因。 |
| 模型与初始化 | DeepSeek-V2-Lite、JoyAI-LLM Flash、GLM-4.7-Flash、Moonlight-16B-A3B；附录另测两个小型 GQA model。 |
| 数据与任务 | 合成 multi-theme conversation；debug-gym mini_nightmare；constructed arithmetic prompt；随机 span edits。 |
| 系统配置 | SGLang 0.5.2；H100/H200；JoyAI/GLM 使用 H200 TP=4，Moonlight 使用 2×H100，DSv2-Lite 使用 1×H100。 |
| 框架基座 / paper base | SGLang RadixCache、Hugging Face transformers offline replay、debug-gym。 |
| 框架版本与证据来源 | 版本来自 Appendix W；公开 Leyline patch 链接未能定位。 |
| 框架改动范围 | 约 200 LOC RadixCache patch、content-hash side index、rotation kernel、Policy interface 与 manifest warm-start。 |
| 未披露项 | 公开仓库、完整 production trace、联合 mechanism×policy ablation、GQA/MHA production kernel、跨 threshold 统计。 |

## 证据链强度评估

### 强证据

- `AMORTIZE` 与 `FORGET` 的语义契约写得明确，尤其说明了前者保留原 span 对 downstream state 的影响。
- $\delta$-rotation 的代数条件、修改 tensor、radix integration 和多 directive composition 均有具体规格。
- 机制三臂位于同一 SGLang scheduler，Table 3 把 cache hit、并发和 latency 分开报告。

### 中等强度证据

- 四个 MLA 模型的 replay 和 random edit stress 支持一定架构宽度；长期数值只到 50 steps / 100 chained rotations。
- debug-gym policy 结果方向清楚，样本少、收益集中在两个任务，适合作为可行性信号。
- 单条低-hit 真实 trace 的 full configuration 有 5.3% 改善，约 95% timing win 来自 fp32 rotation mitigation；Role B 在该轨迹上 timing-neutral。另一条高-hit trace 无收益，进一步限制了 cache reuse 的端到端归因。

### 需要谨慎的推论

- “可编辑 KV Cache”不能直接解释为真正遗忘；`AMORTIZE` 有意保留原内容影响。
- policy solve-rate 增益没有运行 splice，不能写成 Leyline kernel 的质量收益。
- GQA / MHA 上的 rotation algebra 可成立，模型状态语义和高效实现仍需 boundary recomputation。
- 论文对 artifact release 的两处描述不一致，当前缺少可定位的官方代码仓库。

## OpenReview / 审稿意见吸收

- Page type: not-found
- Match confidence: high
- Observed at: 2026-07-14
- Venue status: arXiv v1；未发现公开会议接收或投稿信息。
- Public reviews: 未发现可可靠匹配的公开 reviewer comments。
- Ratings / confidence: 无。
- Reviewer consensus: 暂无。
- Main criticisms: 本地审计重点是 `AMORTIZE` 的持久语义、两条证据链未联合、MLA 架构范围、bf16 数值 floor、并发 candidate-filter 和 artifact 缺口。
- Author response: 暂无公开 rebuttal。
- 对可信度的影响: 核心接口和代数机制可直接复查；性能与 agent-level 价值仍需要公开实现、联合消融和更广 workload 复验。

## 本地讨论补充

### 1. 讨论收敛点

- Agent Workflow 的 cache state 具有语义版本。message list、rendered tokens、KV pages、workflow node input 和 speculative branch 需要绑定到同一 version；任一层发生 edit 后，其他层必须选择校正、定向重算或失效。
- `AMORTIZE` 更接近“修改显式历史并保留已经传播的内部状态”，适合上下文展示压缩和计算摊销。纠错、隐私删除与安全回滚需要 `FORGET`。
- SPORK rejected probe 属于 branch-local 临时状态，通常直接 cancel/free；Leyline 更适合已经 commit 到 canonical history 的 span。

### 2. 修正后的理解

- Leyline 提供的是 agent→cache mutation control plane。LMCache 一类系统可继续负责 block storage/movement，policy 负责 span 和 mode，架构 kernel 负责位置与状态正确性。
- splice 节省 re-prefill compute 和 KV writes，Role B 会把 slots pin 在 trie 中，因此它不直接降低峰值 KV pool occupancy。
- 三篇组合时，Grape 管已知 task 计算，SPORK 管在线 action speculation，Leyline 管跨 turn state mutation；它们共享 GPU batch 与 KV pages，需要统一 admission 和版本协议。

### 3. 后续复验指标

- 同一真实 agent trace 上的 `keep_all`、re-prefill truncation、`AMORTIZE` splice truncation、`FORGET` 四臂对照。
- edited-prompt logits、full-context logits 与 Leyline logits 的 KL / common-prefix / task outcome，并按 logit margin 分桶。
- message-boundary 与 mid-message edit、正负 $\Delta$、多 directive、1000+ turn 的累计漂移。
- GQA / MHA boundary recomputation 的额外 FLOPs、cache hit、P50/P95/P99 与 peak KV occupancy。
- Grape incremental prefill 已经消费某个 span 后发生 Leyline edit 时，依赖 node 的定向失效与重算成本。

## 主要启发

- Agent runtime 需要 prompt channel 之外的 cache-edit channel，把 policy 的语义知识转为 serving 可执行的 directive。
- KV 优化需要同时说明计算等价、缓存状态语义和错误回退；位置正确只覆盖其中一层。
- `mode` 是重要的 API 设计：性能路径与真正遗忘使用不同 contract，调用方需要显式选择。
- 跨论文系统可以把 TaskFlow、speculative branch 和 KV directive 统一到一个带版本、提交和回滚的 execution IR。

## 局限

1. 高效 kernel 的主证据集中在 MLA；GQA / MHA、非 RoPE 位置编码和 sequence-compressed KV 仍待实现。
2. `AMORTIZE` 保留原 span 对 downstream hidden state 的影响，适用语义明显窄于任意 context editing。
3. Policy leg 只有 8 tasks×4 seeds、一个截断 threshold，且没有与 splice 联合运行。
4. 合成 mechanism workload 为 edit 后相同 suffix 提供了较大 headroom；高 radix-hit 真实轨迹显示收益可归零。
5. 100 次 chained rotations 的 relative-L2 约为 $2.6\times10^{-2}$，千次级长期轨迹未测；bf16 KV floor 仍会造成少量 argmax flip。
6. Splice 不降低 peak allocator watermark；cross-tenant warm-start 只在窄配置激活，更高 tenant/concurrency 出现 candidate-filter degenerate。
7. PIN、SHARE、DECAY、SCRATCH 等后续 verbs 仍是设计议程，并引入跨 tenant 泄漏、恶意 pin 与资源耗尽风险。
8. 公开 artifact 当前不可定位，约 200 LOC patch、manifest 和策略代码难以独立复验。

## 跨论文关系

- 与 [Grape](/papers/2026-07-13-grape-micro-task-agentic-workflow-serving/)：Grape 在预声明 DAG 中提前执行下游 prefill，并管理 append-only micro-task KV；Leyline 处理 canonical history 已发生的 span mutation。二者组合需要更新 sequence length、partial-edge 进度、batch cost model、KV ownership 和依赖 node version。
- 与 [SPORK](/papers/2607.03333-spork-self-speculative-agentic-inference/)：SPORK 在 ReAct turn 内 fork branch 预测下一次只读工具调用；Leyline 在 turn boundary 修改已提交 history。Probe miss 适合 branch-local cancel，已提交的 stale observation 才进入 directive 路径。
- 与 [Span Query](/papers/2511.02749-span-queries-cache-attention-locality/)：Span Query 让客户端声明可重排 message spans，以 expression tree 提升 locality；Leyline 让 policy 声明可替换 span，并在 cache 层处理 mutation 后的位置与重算语义。
- 与 [ThunderAgent](/papers/2602.13692-thunderagent-program-aware-agentic-inference/)：ThunderAgent 管 tool wait 期间的 KV pause / restore 和 program lifecycle；Leyline 增加 message edit 对 KV state 的显式控制。完整 runtime 需要同时管理等待、编辑、版本和回收。
- 与 Irminsul：同一作者团队与同一 $\delta$-rotation 基础。Irminsul 由 content hash 触发自然位置迁移的 KV reuse，Leyline 由 agent policy 触发主动 mutation。
- 综合关系见 [Agent Workflow Serving Stack](/papers/2026-07-14-agent-workflow-serving-grape-spork-leyline/)。

## Reference Intake Brief

### Target

- Intended target system: 新增 Leyline 独立论文笔记，并作为 Agent Workflow serving 技术分享的 cache-mutation 层。
- Existing related assets: `content/utility/papers-index.md`；[Grape](/papers/2026-07-13-grape-micro-task-agentic-workflow-serving/)、[SPORK](/papers/2607.03333-spork-self-speculative-agentic-inference/)、[Span Query](/papers/2511.02749-span-queries-cache-attention-locality/)、[ThunderAgent](/papers/2602.13692-thunderagent-program-aware-agentic-inference/)。
- Proposed form: 新建独立 Markdown 文档，更新索引当前收录行、作者档案与标签，并在对应论文关系章节补充双向关系。

### Reusable Elements

1. `(span, replacement, mode)` 作为 agent→cache 的语义编辑协议。
2. `AMORTIZE` / `FORGET` 对位置摊销与真正遗忘的显式分层。
3. MLA $\delta$-rotation、radix reinsertion 与 message-edit cache headroom 的机制分析。

### Risks

- Copyright/over-copying: 只保留接口、公式、关键实验数字和本地分析。
- Unsourced or unverifiable claims: artifact 状态按“官方链接当前不可定位”记录，不把未来发布计划写成已开源事实。
- Tone/brand mismatch: 区分论文事实、作者主张和本地组合设计。
- Safety/compliance issues: 明确 `AMORTIZE` 不满足敏感信息删除；跨 tenant directive 需要授权与隔离。
- Overlap with existing assets: 单篇保留机制证据，综合文档负责 Grape/SPORK/Leyline 的统一 serving stack。

### Skipped

| Material | Reason |
| --- | --- |
| 公开 reviewer comments | 未发现可可靠匹配的公开审稿页。 |
| 官方代码仓库 | arXiv 未给链接，论文附录表述冲突，当前搜索无法定位。 |
| PIN / SHARE / DECAY / SCRATCH 实验 | 论文只提出未来接口，没有当前实证。 |

### Recommendation

Decision: merge

Why: Leyline 把 agent history mutation 转为可审计的 cache directive，并明确了位置摊销与语义遗忘的 contract。MLA splice 的 hit-rate 与延迟证据值得归档；policy×mechanism、跨架构和长期一致性缺口已保留。
