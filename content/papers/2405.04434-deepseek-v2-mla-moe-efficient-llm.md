# DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model 论文笔记

First-Archived-At: 2026-06-23 18:25
Updated-At: 2026-06-24 20:20

## Source

- Title: DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model
- arXiv: https://arxiv.org/abs/2405.04434
- HTML: https://arxiv.org/html/2405.04434v5
- PDF: https://arxiv.org/pdf/2405.04434
- TeX Source: https://export.arxiv.org/e-print/2405.04434
- Code/Project: https://github.com/deepseek-ai/DeepSeek-V2
- Model: [DeepSeek-V2](https://huggingface.co/deepseek-ai/DeepSeek-V2)；[DeepSeek-V2-Chat](https://huggingface.co/deepseek-ai/DeepSeek-V2-Chat)；[DeepSeek-V2-Lite](https://huggingface.co/deepseek-ai/DeepSeek-V2-Lite)
- OpenReview / Review page: [OpenReview CoRR / DBLP metadata](https://openreview.net/forum?id=MwHAn6R7OS)；该页为 CoRR / DBLP 元数据记录，未发现 official reviews。
- Authors: Wenfeng Liang, Huazuo Gao, Wangding Zeng
- Organization: DeepSeek-AI；arXiv lists DeepSeek-AI and 156 other authors；appendix credits Huazuo Gao and Wangding Zeng for MLA architecture innovations.
- Submitted: 2024-05-07
- Current version read: v5, last revised 2024-06-19
- Subjects: Computation and Language (cs.CL); Artificial Intelligence (cs.AI)

## 作者与关系

- DeepSeek-AI author group: DeepSeek AI.
- [Wenfeng Liang](/authors/wenfeng-liang/): DeepSeek AI.
- [Huazuo Gao](/authors/huazuo-gao/) and [Wangding Zeng](/authors/wangding-zeng/): DeepSeek AI.

## 一句话结论

DeepSeek-V2 把 `MLA (Multi-head Latent Attention，多头潜变量注意力)` 的 `KV (Key-Value，注意力键值缓存)` 压缩和 [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/) 的稀疏激活组合成 236B total / 21B active 的 `MoE (Mixture-of-Experts，混合专家)` 模型，在 8.1T tokens 预训练和 `SFT (Supervised Fine-Tuning，监督微调)` / `RL (Reinforcement Learning，强化学习)` 后达到 2024 年开放模型第一梯队，同时相对 DeepSeek 67B 节省 42.5% 训练成本、减少 93.3% KV cache，并把最大生成吞吐提升到 5.76 倍。

## 阅读目标与判断边界

归档关注：

1. MLA 如何把长上下文服务中的 KV cache 从主要瓶颈降下来。
2. [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/) 如何把参数规模扩大到 236B total，同时保持 21B active 的训练和推理成本。
3. DeepSeek-V2 到 V3/R1/V4 的架构连续性。
4. 论文中的效率、能力和 RL alignment 证据强弱。

判断边界：

- 论文是 2024 年技术报告；baseline 强度需要按当时模型生态解释。
- 训练数据、reward model 数据、HAI-LLM 内部框架和 vLLM 优化没有完整开源。
- 长上下文证据主要来自 NIAH 检索测试，复杂长文档推理和 agent trajectory 覆盖不足。

## 术语预备

- `MHA (Multi-Head Attention，多头注意力)`: 每个 head 独立保存 key / value。
- `MQA (Multi-Query Attention，多查询注意力)`: 多个 query heads 共享 key / value，降低 KV cache。
- `GQA (Grouped-Query Attention，分组查询注意力)`: 在 MHA 和 MQA 之间折中。
- `RoPE (Rotary Position Embedding，旋转位置编码)`: 通过旋转变换编码 token 位置。
- `FFN (Feed-Forward Network，前馈网络)`: Transformer block 中 attention 后的前馈子层。
- `GRPO (Group Relative Policy Optimization，组相对策略优化)`: DeepSeekMath / DeepSeek-R1 线使用的 critic-free RL 方法。
- `ZeRO-1 (Zero Redundancy Optimizer stage 1，优化器状态切分)`: 数据并行中的 optimizer state partitioning。

## 论文脉络

### 1. 研究问题、背景和价值

大模型扩参会同时推高训练 FLOPs、推理 KV cache、长上下文内存成本和在线服务吞吐压力。Dense 67B 级模型在能力上已经有竞争力，但训练和服务成本很难继续线性扩展。DeepSeek-V2 的目标是在开放权重模型中同时追求能力、训练成本和推理效率。

### 2. 已有解决方案与不足

MHA 表达力强但 KV cache 重；MQA 和 GQA 能省缓存但会牺牲一部分能力。传统 MoE 用稀疏激活降低 active compute，但会引入专家专门化不足、路由不均衡、跨设备通信和 token dropping 风险。DeepSeek-V2 把这两个瓶颈分别拆开：attention 用 latent KV 压缩，FFN 用 [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/) 扩大 total 参数并控制 active 参数。

### 3. 作者可能的思考路径

DeepSeek 67B 已经给出可用 dense baseline，继续扩大 dense model 会同时推高训练 FLOPs、KV cache、服务显存和吞吐成本。只把 FFN 换成 MoE 可以降低 active compute，但 decode 阶段的 KV cache 仍会限制在线服务；只用 MQA/GQA 压 KV，又会牺牲一部分 attention 表达。

因此 V2 的思路会先把成本中心拆开：attention 侧需要一种比 MQA/GQA 更保留表达力的 cache 压缩；FFN 侧可以继承 [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/) 的 fine-grained experts / shared experts 来扩大 total capacity；路由和通信侧还要给 expert parallelism 加上扇出上限，否则 MoE 的 all-to-all 会吞掉稀疏激活节省下来的成本。

post-training 则沿用 DeepSeekMath 已验证的 GRPO 方向。V2 的问题在于让 MLA、DeepSeekMoE、device-limited routing 和 SFT/RL 在同一个 bilingual frontier-scale 模型上同时成立，单点算法创新只覆盖其中一层。

### 4. 核心假设或切入点

低秩联合压缩 KV 仍可保留强 attention 表达；RoPE 可以通过 decoupled key/query 分支保留位置能力；细粒度 routed experts 加 shared experts 能提升专家专门化；device-limited routing 与 balance losses 可以把 MoE 通信控制在可训练范围内。

### 5. 方法 / 系统 / 理论框架

#### MLA：从 per-head KV cache 到 latent KV cache

DeepSeek-V2 的设计先面对 decode 阶段的显存和带宽压力。标准 `MHA (Multi-Head Attention，多头注意力)` 在每个 token 上为每个 head 保存完整 key 和 value；序列越长，KV cache 越快变成服务吞吐瓶颈。`MQA (Multi-Query Attention，多查询注意力)` 和 `GQA (Grouped-Query Attention，分组查询注意力)` 通过共享 KV 降低 cache，但会直接减少 KV 表达自由度。V2 新提出的 `MLA (Multi-head Latent Attention，多头潜变量注意力)` 选择另一条路线：先把当前 token 的 hidden state 下投影成一个低维 latent KV，再由这个 latent 生成各个 head 的 content key 和 value。推理时缓存 compressed latent KV，避免保存展开后的多 head KV，所以 cache 大小从完整 KV 的 head 维度降到 latent 维度。V2 的配置是 60 layers、hidden size 5120、128 attention heads、head dimension 128，KV compression dimension 512，query compression dimension 1536。

#### Decoupled RoPE：把位置能力放进小分支

MLA 的关键难点在 RoPE。若对 content key 直接施加 `RoPE (Rotary Position Embedding，旋转位置编码)`，上投影矩阵和当前位置相关的旋转矩阵会夹在 query 侧矩阵之间，推理时无法把 key 的上投影吸收到 query projection 里，低秩 KV 压缩会在 decode 阶段重新引入展开成本。V2 用 decoupled RoPE 把 key/query 拆成 content 分支和 position 分支：content 分支从 latent KV 生成，不直接承载 RoPE；position 分支单独生成 64 维 RoPE key/query。最终每个 head 用 `[content; position]` 做 attention。这样 cache 中只需要保存 compressed latent KV 和 decoupled RoPE key，content KV 可以在需要时由 latent 还原，位置能力由小的 RoPE 分支保留。这是 V2 相对传统 MHA/GQA 的核心新机制。

#### Projection absorption：把历史 key 展开移到当前 query

这个“把 key 上投影吸收到 query 侧”的优化可以直接从点积展开看出来。为了贴近 PyTorch 实现，下面统一用行向量写法。设历史 token 的 compressed latent KV 为 $c_i \in \mathbb{R}^{1 \times d_c}$，content key 由上投影得到：

$$
k_i^{\mathrm{content}} = c_i W^{UK},
$$

其中 $W^{UK} \in \mathbb{R}^{d_c \times d_h}$。当前 token 的 content query 为 $q_t^{\mathrm{content}} \in \mathbb{R}^{1 \times d_h}$。朴素 attention score 会先为每个历史 token 展开 key：

$$
s_i = q_t^{\mathrm{content}} (k_i^{\mathrm{content}})^\top
    = q_t^{\mathrm{content}} (c_i W^{UK})^\top.
$$

利用转置关系 $(AB)^\top = B^\top A^\top$ 和矩阵乘法结合律，可以把上式改写成：

$$
s_i = q_t^{\mathrm{content}} (W^{UK})^\top c_i^\top
    = \left(q_t^{\mathrm{content}} (W^{UK})^\top\right)c_i^\top.
$$

于是 decode 时只需要对当前 query 计算一次 $q_t^{\mathrm{latent}} = q_t^{\mathrm{content}} (W^{UK})^\top$，再让它和 cache 中所有历史 latent $c_i$ 做点积。低效路径是对每个历史 token 都算 $c_i W^{UK}$；高效路径是把 $(W^{UK})^\top$ 移到当前 query 侧，历史 cache 保持 compressed latent 形态。value 侧也有类似改写：先在 latent 空间按 attention weight 求和，再通过 value 上投影和 output projection 还原，而不必长期缓存完整 per-head value。

RoPE 会破坏这一步的原因在于旋转矩阵依赖位置。若 content key 直接带 RoPE，score 会包含类似 $q_t R_t R_i^\top (W^{UK})^\top c_i^\top$ 的项，其中 $R_i$ 随历史 token 位置 $i$ 改变，$(W^{UK})^\top$ 便无法变成一个只作用在当前 query 上的一次性固定投影。decoupled RoPE 的设计就是让 content 分支保持可吸收结构，把位置信息放进单独的小 position 分支。每个 head 最终计算的是 content score 加 position score：

$$
[q^{\mathrm{content}}; q^{\mathrm{position}}]
[k^{\mathrm{content}}; k^{\mathrm{position}}]^\top
=
q^{\mathrm{content}} (k^{\mathrm{content}})^\top
+ q^{\mathrm{position}} (k^{\mathrm{position}})^\top.
$$

因此 MLA 中的 decoupled RoPE 对整个 head 的处理方式不同于传统 RoPE：RoPE 只服务 position 子空间，content 子空间继续服务 latent KV 压缩和 decode-time projection absorption。

#### DeepSeekMoE FFN：扩大 total capacity 并控制 active compute

FFN 侧的压力从参数容量转到路由可运行性。V2 沿用并规模化 [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/)：除第一层外，Transformer block 的 FFN 都换成稀疏专家层，每层包含 2 个 shared experts 和 160 个 routed experts，每个 token 激活 6 个 routed experts。shared experts 处理所有 token，吸收通用模式；routed experts 只处理被 gate 选中的 token，扩大 total capacity 并保持 active compute 可控。对第 $t$ 个 token，论文把 MoE-FFN 写成：

$$
h'_t
= u_t + \sum_{i=1}^{N_s}\mathrm{FFN}^{(s)}_i(u_t)
+ \sum_{i=1}^{N_r} g_{i,t}\mathrm{FFN}^{(r)}_i(u_t),
\quad
s_{i,t}=\mathrm{Softmax}_i(u_t^\top e_i).
$$

其中 $g_{i,t}$ 只在 affinity score $s_{i,t}$ 进入 top-$K_r$ routed experts 时保留，其他专家 gate 置零。V2 的具体配置是 $N_s=2$、$N_r=160$、$K_r=6$，每个 expert 的 intermediate hidden dimension 为 1536，最终形成 236B total / 21B active 参数。

#### Device-limited routing：限制 MoE 通信扇出

直接 top-$K$ 路由会把每个 token 发往若干专家；在 expert parallelism 下，通信频次取决于这些专家覆盖了多少设备。DeepSeekMoE 的 fine-grained expert segmentation 提高了专家专门化，也会让跨设备扇出更容易成为瓶颈。V2 加入 device-limited routing：先在设备层选出包含高 affinity 专家的 $M$ 个设备，再只在这 $M$ 个设备上的 experts 中做 top-$K_r$ 选择。训练中 routed experts 均匀部署到 $D=8$ 个设备，$M=3$，因此单个 token 的 MoE all-to-all 扇出有硬上限。论文报告当 $M\ge 3$ 时，该受限路由的效果大致贴近 unrestricted top-$K$。

#### Balance losses：同时约束专家、设备和接收端

路由受限只能约束发送端扇出，负载仍可能集中到少数 experts、少数设备或少数接收端。V2 因此叠加三类 balance loss。expert-level balance loss 约束专家使用：

$$
\mathcal{L}_{\mathrm{ExpBal}}
= \alpha_1 \sum_{i=1}^{N_r} f_iP_i,\quad
f_i=\frac{N_r}{K_rT}\sum_{t=1}^{T}\mathbb{1}\{t\to i\},\quad
P_i=\frac{1}{T}\sum_{t=1}^{T}s_{i,t}.
$$

device-level balance loss 把专家负载聚合到设备组 $\mathcal{E}_i$，约束不同设备的计算量：

$$
\mathcal{L}_{\mathrm{DevBal}}
= \alpha_2 \sum_{i=1}^{D} f'_iP'_i,\quad
f'_i=\frac{1}{|\mathcal{E}_i|}\sum_{j\in\mathcal{E}_i}f_j,\quad
P'_i=\sum_{j\in\mathcal{E}_i}P_j.
$$

communication balance loss 进一步约束接收端通信量：

$$
\mathcal{L}_{\mathrm{CommBal}}
= \alpha_3 \sum_{i=1}^{D} f''_iP''_i,\quad
f''_i=\frac{D}{MT}\sum_{t=1}^{T}\mathbb{1}\{t\to \mathcal{E}_i\},\quad
P''_i=\sum_{j\in\mathcal{E}_i}P_j.
$$

其中 $T$ 是一个序列内的 token 数。device-limited routing 保证每个设备最多发送约 $MT$ 个 hidden states，communication balance loss 鼓励每个设备也接收约 $MT$ 个 hidden states。V2 的 balance factors 是 $\alpha_1=0.003$、$\alpha_2=0.05$、$\alpha_3=0.02$。

#### Token dropping：用训练期硬容量处理短时过载

balance loss 仍是软约束，训练中还会出现短时负载偏斜。V2 用 device-level token dropping 处理这个尾部成本：先按设备计算平均计算预算，相当于 capacity factor 为 1.0；每个设备上超出预算时，丢弃 affinity score 最低的 token，直到回到预算内。为了保留完整序列监督，约 10% training sequences 的 token 永远不丢。论文的正式评测阶段不丢 token，训练阶段使用 token dropping 主要服务加速。

#### Training system：让并行策略配合 MLA/MoE

训练系统的配置服务于这套 MoE/MLA 结构。数字密集部分按新 SOP 拆成表格。

| 维度 | 配置 |
| --- | --- |
| 训练框架 | DeepSeek HAI-LLM internal framework |
| 训练硬件 | H800 cluster；论文未披露完整 GPU 数、节点数和每节点卡数 |
| 并行方式 | 16-way zero-bubble pipeline parallelism；8-way expert parallelism；ZeRO-1 optimizer-state partitioning |
| Tensor parallelism | 训练阶段不使用 TP；部分 operators recompute 以节省 activation memory |
| 通信 / kernel | shared experts 计算与 expert parallel all-to-all overlap；定制 communication、routing、multi-expert fused linear CUDA kernels；改进版 [FlashAttention-2](/papers/2307.08691-flashattention-2-parallelism-work-partitioning/) |

| 维度 | 配置 |
| --- | --- |
| Tokenizer | `BBPE (Byte-level Byte-Pair Encoding，字节级 BPE)`；vocab 100K |
| Pretraining data | 8.1T bilingual / multi-source tokens |
| Sequence / batch | sequence length 4K；batch size 2304 -> 9216 ramp |
| Optimizer | AdamW；beta1 0.9；beta2 0.95；weight decay 0.1；max LR 2.4e-4；2K warmup |
| 训练时间 / 成本 | 每 1T tokens 训练 172.8K H800 GPU hours；完整 wall-clock 与总美元成本未披露 |

#### Long context：YaRN 只作用在 position 分支

长上下文扩展的关键在 position 分支。MLA 已经把 content 分支和 RoPE position 分支拆开，所以 YaRN 只需要作用到承载 RoPE 的 decoupled shared key $k_t^R$，content latent KV 路径继续保持 cache 压缩结构。V2 用 [YaRN](https://arxiv.org/abs/2309.00071) 将默认 4K context 扩展到 128K，设置 scale $s=40$、$\alpha=1$、$\beta=32$，target maximum context length 160K。由于 MLA attention 结构和原始 YaRN 假设存在差异，V2 额外调整 length scaling factor 来控制 attention entropy，论文给出的形式是 $\sqrt{t}=0.0707\ln{s}+1$。长上下文继续训练只跑 1000 steps，sequence length 32K，batch 576；评测在 128K NIAH 上显示可用。

#### Post-training：SFT 与两阶段 GRPO

Chat 后训练先用 1.5M instruction instances 做 `SFT (Supervised Fine-Tuning，监督微调)`，其中 1.2M helpfulness、0.3M safety，训练 2 epochs，learning rate $5\times10^{-6}$。随后使用 `GRPO (Group Relative Policy Optimization，组相对策略优化)` 降低 RL 成本：它省去与 policy 同规模的 critic，让同一 prompt 的一组 sampled outputs 彼此提供 baseline。对问题 $q$，旧策略 $\pi_{\theta_{\mathrm{old}}}$ 采样 $\{o_i\}_{i=1}^{G}$，advantage 用组内 reward 标准化：

$$
A_i=\frac{r_i-\mathrm{mean}(\{r_1,\ldots,r_G\})}
{\mathrm{std}(\{r_1,\ldots,r_G\})}.
$$

策略更新使用 clipped ratio 加 reference KL penalty：

$$
\min\!\left(
\frac{\pi_\theta(o_i|q)}{\pi_{\theta_{\mathrm{old}}}(o_i|q)}A_i,\,
\mathrm{clip}\!\left(
\frac{\pi_\theta(o_i|q)}{\pi_{\theta_{\mathrm{old}}}(o_i|q)},1-\epsilon,1+\epsilon
\right)A_i
\right)
-\beta D_{\mathrm{KL}}(\pi_\theta\|\pi_{\mathrm{ref}}).
$$

论文中的 KL 项采用基于 ratio 的正向 KL 估计形式：

$$
D_{\mathrm{KL}}(\pi_\theta\|\pi_{\mathrm{ref}})
=
\frac{\pi_{\mathrm{ref}}(o_i|q)}{\pi_\theta(o_i|q)}
-\log\frac{\pi_{\mathrm{ref}}(o_i|q)}{\pi_\theta(o_i|q)}
-1.
$$

RL 分两阶段。第一阶段是 reasoning alignment，针对 code/math prompt 训练 $RM_{\mathrm{reasoning}}$，令 $r_i=RM_{\mathrm{reasoning}}(o_i)$；论文强调数学和代码能力可在更长 RL steps 中继续提升。第二阶段是 human preference alignment，用 helpful、safety、rule 三类 reward 合成最终反馈：

$$
r_i=c_1RM_{\mathrm{helpful}}(o_i)
+c_2RM_{\mathrm{safety}}(o_i)
+c_3RM_{\mathrm{rule}}(o_i).
$$

reward model 从 DeepSeek-V2 Chat SFT 初始化，并用 point-wise 或 pair-wise loss 训练；代码偏好数据依赖 compiler feedback，数学偏好数据依赖 ground-truth labels。RL 系统侧采用 hybrid engine，训练和推理使用不同并行策略，rollout 后端使用 vLLM 大 batch 推理，并通过 CPU/GPU offload 调度平衡训练速度和显存占用。需要保留的边界是：论文披露了 GRPO 目标和 reward 来源，但没有完整给出 group size、$\epsilon/\beta$ 实际值、reward coefficients 和 reward 数据规模。

### 6. 结论链条

MLA 降低 KV cache，[DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/) 降低 active compute，device-limited routing 控制通信，SFT/RL 提升 chat、代码和数学能力。四部分合起来让 V2 在 2024 年开放模型中同时具备较高能力和较低服务成本。

## 关键实验/定理

### 结果 1：base model 能力

- 设置：内部统一评测；对比 DeepSeek 67B、Qwen1.5 72B、Mixtral 8x22B、LLaMA3 70B。
- Baseline：上述 2024 年主流开放模型。
- 指标：MMLU、MATH、CMMLU、C-Eval 等。
- 结果：MMLU 78.5，MATH 43.6，CMMLU 84.0，C-Eval 81.7，多数项明显超过 DeepSeek 67B。
- 解读：21B active 参数进入当时开源前列，中文和数学优势明显。

### 结果 2：训练与推理效率

- 设置：DeepSeek 67B dense baseline；H800 cluster；服务流量分布。
- Baseline：DeepSeek 67B。
- 指标：每 1T tokens 训练 GPU hours、生成吞吐、prompt throughput。
- 解读：MLA + MoE 的系统收益是论文最核心证据。

结果：

| 指标 | DeepSeek-V2 | DeepSeek 67B baseline |
| --- | ---: | ---: |
| 每 1T tokens 训练成本 | 172.8K H800 GPU hours | 300.6K H800 GPU hours |
| 最大生成吞吐 | 5.76x baseline | 1.00x |
| Prompt throughput | > 100K tokens/s | 未列出 |

### 结果 3：MLA 消融

- 设置：7B dense 比 MQA/GQA/MHA；MoE 比 MLA vs MHA。
- Baseline：MHA、MQA、GQA。
- 指标：KV cache / token、BBH、MMLU、CMMLU。
- 结果：large MoE KV cache/token 34.6K vs MHA 860.2K；BBH 50.7 vs 46.6，MMLU 59.0 vs 57.5，CMMLU 62.5 vs 60.7。
- 解读：MLA 在该设置下降低缓存同时保持或提升能力；small MoE 的 C-Eval 略低，说明收益有任务差异。

### 结果 4：长上下文

- 设置：YaRN 后 32K 训练，NIAH 评估到 128K。
- Baseline：原始 4K context 模型。
- 指标：needle retrieval accuracy。
- 结果：论文图示在 128K 内表现稳定。
- 解读：支持长上下文可用性，但 NIAH 主要验证检索。

### 结果 5：chat / RL

- 设置：SFT/RL 对比 Qwen1.5、Mixtral、LLaMA3、DeepSeek 67B Chat。
- Baseline：SFT checkpoint 和外部 chat models。
- 指标：MT-Bench、AlpacaEval 2.0、AlignBench、HumanEval、LiveCodeBench、MATH。
- 结果：MT-Bench 8.97，AlpacaEval 2.0 LC win rate 38.9，AlignBench 7.91；HumanEval 81.1，LiveCodeBench 32.5，MATH 53.9。
- 解读：RL 主要改善开放生成、代码和数学；BBH、MMLU、C-Eval 从 SFT 到 RL 有小幅下降，体现 alignment tax。

### 实验设置与 baseline 审计

| 维度 | 记录 |
| --- | --- |
| 模型与初始化 | 236B total / 21B active DeepSeek-V2；Lite 版本 15.7B total / 2.4B active |
| 数据与任务 | 8.1T bilingual / multi-source pretraining tokens；SFT 1.5M instruction instances |
| RL / 训练配置 | GRPO；reasoning alignment 与 human preference alignment 两阶段；reward 由 reasoning / helpful / safety / rule models 组成；rollout group size、reward coefficients、KL/clip、reward 数据规模未完整披露 |
| 系统配置 | H800 cluster；HAI-LLM；16-way zero-bubble pipeline；8-way expert parallelism；ZeRO-1；改进 FlashAttention-2；训练阶段不使用 TP |
| 技术报告训练配置 | pretraining 8.1T tokens；sequence length 4K；batch 2304 -> 9216；每 1T tokens 172.8K H800 GPU hours；长上下文继续训练 1000 steps、sequence length 32K、batch 576 |
| 未披露项 | 完整 GPU 数、节点数、每节点卡数、总 wall-clock、总 GPU hours、美元成本、RL group size / reward coefficients / 数据规模 |
| 评测协议 | 内部统一评测与公开 benchmark 混合 |
| 统计报告 | 缺少 seed、误差线和显著性检验 |
| Baseline 强度 | 2024 年语境下强，包含 LLaMA3 70B、Mixtral 8x22B、Qwen1.5 72B、DeepSeek 67B；当前阅读时应标注时间边界 |
| 结论边界 | 效率结论对 DeepSeek 内部实现和服务分布敏感；外部 HF path 慢于内部优化实现 |

## 证据链强度评估

### 强证据

- MLA KV cache 公式与消融清楚。
- DeepSeek 67B 训练/吞吐对比直接支撑效率主张。
- V2-Lite 与 V2 模型权重公开，便于社区复验部分设计。

### 中等强度证据

- 128K context 主要由 NIAH 支撑，适合说明检索可用性。
- RL preference alignment 有 benchmark 结果，GRPO 目标和 reward 来源已披露；group size、实际 KL/clip 超参数、reward coefficients 和 reward 数据规模仍缺。

### 需要谨慎的推论

- 与闭源模型或 API 模型的横向比较。
- 在线 RL 优于离线 RL 的泛化判断。
- V2 架构对后续更长 agent workload 的充分性。

## OpenReview / 审稿意见吸收

- Venue status: CoRR / arXiv technical report；OpenReview 页面为 DBLP/CoRR 元数据记录。
- Public reviews: OpenReview API 显示 replyCount 为 0，未发现 official review。
- Ratings / confidence: 未发现。
- Reviewer consensus: 未发现。
- Main criticisms: 无公开 reviewer comments；本地审计主要依据论文披露缺口。
- Author response: 未发现。
- 对可信度的影响: 可作为 DeepSeek 架构谱系的关键原始报告使用；正式审稿认可度暂无公开证据。

## 本地讨论补充

### 1. 讨论收敛点

DeepSeek-V2 是理解 DeepSeek 后续路线的关键前置：V3 继承 MLA 和 [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/)，再引入 auxiliary-loss-free balance、MTP 和 FP8；V4 则把 KV/attention efficiency 推到 million-token context。

### 2. 修正后的理解

V2 的主要贡献应写成 architecture + systems co-design，MoE 模型规模只是其中一层。MLA 降低服务内存，MoE 降低 active compute，device-limited routing 降低通信，GRPO alignment 补能力。

### 3. 后续复验指标

- KV cache / token 与真实 serving memory。
- routing balance、token dropping rate、expert specialization。
- RL 前后 alignment tax。
- NIAH 之外的多跳长文档和长代码评测。

## 主要启发

- KV cache 压缩可以和 MoE 稀疏激活叠加，分别处理 attention memory 和 FFN compute。
- 大模型服务效率需要架构和系统共同设计，单靠 kernel 优化不够。
- GRPO 在 DeepSeek-V2 chat RL 中已经出现，R1 是这条线在 reasoning RL 上的放大。

## 局限

1. 训练数据、reward model 数据、内部 HAI-LLM / vLLM 优化没有完整开源。
2. 缺少 seed、误差线、统计显著性和完整 compute-matched baseline。
3. 128K 证据主要来自 NIAH。
4. RL 细节不足，在线 RL 优于离线 RL 需要独立复验。
5. 公开审稿缺失。

## 跨论文关系

- 与 [2501.12948 DeepSeek-R1](/papers/2501.12948-deepseek-r1-rl-reasoning/)：V2 已使用 GRPO 做 chat RL；R1 在 V3-Base 上把 GRPO / verifiable reward 扩展成 reasoning RL 主线。
- 与 [2026-04-24 DeepSeek-V4](/papers/2026-04-24-deepseek-v4-million-token-context-intelligence/)：V4 的 CSA/HCA、mHC、Muon、deterministic kernels、heterogeneous/on-disk KV cache 是 V2 的 MLA/KV/MoE 效率路线在 1M context 上的后续系统化。
- 与 [2602.15763 GLM-5](/papers/2602.15763-glm-5-agentic-engineering/)：GLM-5 采用 MLA 并讨论 latent KV 的优化器适配；V2 是理解 MLA 原始设计动机的前置材料。
- 与 [2606.04101 UltraEP](/papers/2606.04101-ultraep-rack-scale-moe-load-balancing/)：V2 用 auxiliary losses、device-limited routing、token dropping 控制负载；UltraEP 用 post-gating exact load 做实时重均衡。
- 与 [2605.14220 TIM/VeXact](/papers/2605.14220-training-inference-mismatch-llm-rl/) 和 [2025-09-10 inference determinism](/papers/2025-09-10-defeating-nondeterminism-llm-inference/)：V2 的 vLLM RL rollout、MoE routing、KV compression 后续都需要 sampler/trainer consistency 检查。

## Reference Intake Brief

### Target

- Intended target system: 新增 DeepSeek-V2 独立论文笔记。
- Existing related assets: [2501.12948 DeepSeek-R1](/papers/2501.12948-deepseek-r1-rl-reasoning/), [2026-04-24 DeepSeek-V4](/papers/2026-04-24-deepseek-v4-million-token-context-intelligence/), [2602.15763 GLM-5](/papers/2602.15763-glm-5-agentic-engineering/), [2606.04101 UltraEP](/papers/2606.04101-ultraep-rack-scale-moe-load-balancing/), `papers-index.md`。
- Proposed form: 新建独立 Markdown 文档。

### Reusable Elements

1. MLA 公式与 KV cache 对比。
2. [DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/) scaling relation。
3. V2 -> V3/R1/V4 架构与系统谱系。

### Risks

- Copyright/over-copying: 只摘取关键数值和结构，不复制原表。
- Unsourced or unverifiable claims: 团队作者不拆身份；内部评测按作者报告处理。
- Tone/brand mismatch: 保持技术归档语气。
- Safety/compliance issues: 只记录能力、训练和安全边界，不提供滥用流程。
- Overlap with existing assets: 与 DeepSeek-R1/V4 强重叠，本条定位为架构前置节点。

### Skipped

| Material | Reason |
| --- | --- |
| 公开 reviewer comments | OpenReview 只有 CoRR/DBLP 记录，API 显示无 direct replies。 |

### Recommendation

Decision: merge

Why: DeepSeek-V2 是 DeepSeek V 系列、MLA、[DeepSeekMoE](/papers/2401.06066-deepseekmoe-expert-specialization/)、GRPO chat alignment 和后续 DeepSeek-R1/V3/V4 的关键上游节点。
