# RollArt: Disaggregated Multi-Task Agentic RL Training at Scale 论文笔记

First-Archived-At: 2026-06-30 14:01
Updated-At: 2026-06-30 14:01

## Source

- Title: RollArt: Disaggregated Multi-Task Agentic RL Training at Scale
- arXiv: https://arxiv.org/abs/2512.22560
- HTML v2: https://arxiv.org/html/2512.22560v2
- PDF: https://arxiv.org/pdf/2512.22560v2
- Code/Project: [ROLL repository](https://github.com/alibaba/ROLL)；ROLL README 在 2026-06-16 news 中标注 OSDI'26 RollArt paper available on arXiv
- OpenReview / Review page: [USENIX OSDI 2026 presentation page](https://www.usenix.org/conference/osdi26/presentation/gao)；未发现公开 reviewer comments
- Authors: Wei Gao, Yuheng Zhao, Tianyuan Wu, Shaopan Xiong, Weixun Wang, Dakai An, Lunxi Cao, Dilxat Muhtar, Zichen Liu, Haizhou Zhao, Ju Huang, Siran Yang, Yongbin Li, Wenbo Su, Jiamang Wang, Lin Qu, Bo Zheng, Wei Wang
- Submitted: v1 2025-12-27；v2 2026-06-15
- Current version read: v2, updated 2026-06-15
- Subjects: Distributed, Parallel, and Cluster Computing (cs.DC); Artificial Intelligence (cs.AI); Machine Learning (cs.LG)
- Venue status: USENIX OSDI 2026 presentation page lists the paper and author affiliations; paper TeX uses USENIX camera-ready style and acknowledges shepherd Shadi Noghabi and anonymous reviewers.

## 作者与关系

- [Wei Gao](/authors/wei-gao-hkust/): HKUST; Alibaba Group.
- [Yuheng Zhao](/authors/yuheng-zhao/): HKUST CSE.
- Tianyuan Wu: HKUST CSE.
- [Shaopan Xiong](/authors/shaopan-xiong/): Alibaba Group.
- [Weixun Wang](/authors/weixun-wang-alibaba/): Alibaba Group.
- Dakai An: HKUST.
- Lunxi Cao: HKUST.
- Dilxat Muhtar: Alibaba Group.
- Zichen Liu: Alibaba Group.
- Haizhou Zhao: Alibaba Group.
- Ju Huang: Alibaba Group.
- Siran Yang: Alibaba Group.
- Yongbin Li: Tongyi Lab, Alibaba.
- Wenbo Su: Alibaba Group.
- Jiamang Wang: Alibaba Group.
- Lin Qu: Alibaba Group.
- [Bo Zheng](/authors/bo-zheng-qwen/): Alibaba Group / Qwen Team.
- [Wei Wang](/authors/wei-wang-hkust/): HKUST CSE.

## 一句话结论

RollArt 是一篇把 agentic RL 训练系统从“单 GPU 集群里的 rollout/training 编排”推进到“异构资源池上的多阶段调度”的系统论文：它把训练放在 H800 这类 compute-optimized GPU，把 decode-heavy rollout 放到 H20 这类 bandwidth-optimized GPU，把环境放到 CPU/Kubernetes，把 stateless reward 放到 serverless，并用轨迹级异步 rollout 与 bounded-staleness 权重同步把长尾环境、跨集群传权和 reward 低利用率一起纳入同一个 runtime。

## 阅读目标与判断边界

本笔记关注：

1. RollArt 如何把 agentic RL 的瓶颈拆成 generation、environment、reward、training 四类异构工作负载。
2. 它的资源映射、trajectory-level rollout、serverless reward、bounded-staleness training 四个设计点各自解决哪一类等待或低利用率。
3. 它和已存档的 verl / HybridFlow、Laminar、slime、Seer、Bebop、GLM-5 系统线的差异。
4. 实验是否足以支撑“生产级 agentic RL disaggregation”的主张。

判断边界：

- 论文的核心证据来自 Alibaba / HKUST 的内部 disaggregated H800/H20/CPU/serverless 环境，外部复验依赖相似硬件、serverless reward 平台和大规模 agentic task mix。
- Baseline 中 Sync、Sync+、One-off 和 AReaL 均由作者在 RollArt 代码库中实现或复现；Laminar closed-source，因此论文通过 feature decomposition 讨论差距。
- RollArt 主要评估系统吞吐、step time、time-to-score 和生产稳定性；对最终模型能力、reward hacking、off-policy bias 的长期影响展示较少。
- ROLL 仓库公开，但本文 v2 未在 arXiv Source 中给出逐模块 artifact 映射；后续应检查 USENIX artifact / ROLL release。

## 论文脉络

### 1. 研究问题、背景和价值

Agentic Reinforcement Learning (RL，强化学习) 和常规 RLVR 的系统瓶颈不同。常规数学 reasoning rollout 常可以近似看成长 response generation；agentic RL 还要维护多轮环境状态、执行工具或 sandbox、等待环境反馈，再把整条轨迹交给 rule / code / LLM-as-a-Judge reward。于是一个 training step 同时包含：

1. compute-bound prefill：多轮交互下 prompt history 增长，每次 action 前要处理更长上下文。
2. bandwidth-bound decoding：少轮但长 CoT / long answer 的任务主要受 HBM bandwidth 约束。
3. CPU-heavy environment：Docker / Kubernetes reset、step、sandbox execution 形成重尾延迟。
4. bursty stateless reward：rule/code reward 或固定 reward LLM 在 batch 完成后突然涌入请求，专用 GPU 长期低利用率。
5. large-volume weight update：训练集群更新权重后，rollout 集群需要拿到新版本。

RollArt 的问题定义是：在多任务 agentic RL 中，如何把这些异构 workload 放到合适硬件上，并让它们以轨迹粒度并发推进，减少最慢环境、跨集群传权和低利用 reward GPU 带来的等待。

价值很直接。随着 Kimi K2、DeepSeek-V3.2-Speciale、GLM-5/5.2 这类 agentic model 把 coding、web、tool、GUI、sandbox 放进 post-training，rollout cost 和 environment instability 会主导 wall-clock。系统如果仍按统一 GPU 池 / batch barrier 设计，会让高端训练 GPU、rollout GPU 和 reward GPU互相等待。

### 2. 已有解决方案与不足

论文把已有系统分成几类。

第一类是 monolithic RL post-training framework，例如 veRL / HybridFlow、slime、rLLM。这些系统提供单控制器、stage 组合和 trainer/rollout 编排，但大多把 stage 放在同类 GPU 池内，异构资源映射较弱。

第二类是部分 disaggregation，例如 AWorld / DeepSWE 将环境放到 Kubernetes，但 rollout 和 training 仍绑定较紧，reward 也未完全作为 elastic function 处理。

第三类是 asynchronous RL system，例如 StreamRL、AsyncFlow、SeamlessFlow、AReaL、Laminar。它们解决 trainer/rollout overlap 或 trajectory-level scheduling 的一部分问题，但 RollArt 认为仍缺少以下组合：

1. rollout 内 prefill-heavy / decode-heavy 任务对 H800 / H20 的不同亲和性；
2. environment reset / step 的长尾和 failure isolation；
3. stateless reward 的 serverless offload；
4. bounded-staleness 权重同步在跨集群链路上的 overlap；
5. 生产任务中 domain-level compute profile 对硬件映射的稳定可用性。

### 3. 作者可能的思考路径

可以把作者思路重建为五步。

1. 从生产 trace 看到单次 agentic RL step 并非 generation 单点主导：SWE-bench 成功 step 平均 365.7s，generation 约 54%，training 约 23%，env initialization 约 15%；出现 environment failure 时平均 step 变成 513.3s，env.reset 占 rollout time 的 78%。
2. 对 generation 再细分，发现任务域呈 bimodal turn distribution：FrozenLake / SWE / WebShop 这类多轮任务 prefill-heavy，GEM-Math 这类少轮长思考任务 decode-heavy。H800 和 H20 在同成本下互有优势。
3. 对 environment 观察到 reset / step 都有长尾，batch-level environment interaction 会让快轨迹等待慢轨迹，最高增加 21.3% rollout time。
4. 对 reward 观察到固定 reward LLM 或 stateless reward function 具有 bursty 特征，在 dedicated GPU 上平均利用率只有 7.4% 或 6% 量级，因此适合 serverless。
5. 对跨集群权重同步测量后，发现 Qwen3-32B 权重传输可达几十秒量级，同步等待会吞掉 disaggregation 收益，因此需要异步传权和 bounded staleness。

RollArt 的核心 intuition 是：agentic RL 系统的自然调度单位应从“一个 batch 的 rollout”降到“一条 trajectory”，资源映射单位应从“整个 rollout stage”细化到“任务域或 prefill/decode phase”。

### 4. 核心假设或切入点

核心假设有四条：

1. 多任务 agentic RL 中，任务域的 prefill/decode/turn profile 足够稳定，可以作为 hardware-affinity mapping 的粗粒度输入。
2. trajectory-level rollout 可以吸收 environment straggler，不会让快环境被 batch barrier 阻塞。
3. reward computation 对多数 agentic tasks 是 stateless / bursty，可用 serverless 弹性容量替代长期保留的 dedicated GPU。
4. bounded-staleness async training 的吞吐收益可以抵消一定 policy staleness，$\alpha=1$ 在本文设置中取得较好的 time-to-score。

### 5. 方法 / 系统 / 理论框架

#### 5.1 三层平面：resource / data / control

RollArt 把系统拆成三层：

- Resource plane：resource manager 维护 H800、H20、CPU cluster、serverless endpoint 等资源池，按硬件亲和性把 worker 绑定到合适资源。
- Data plane：pipeline runner 用 `Worker` 和 `Cluster` 抽象把用户逻辑变成训练、生成、环境、reward 四类 role-specific cluster；后端包括 Megatron、vLLM、SGLang、Ray、Kubernetes 和 serverless platform。
- Control plane：rollout scheduler、`LLMProxy`、`EnvManager`、`SampleBuffer` 协调轨迹级 rollout、reward dispatch、training batch consumption 和权重同步。

#### 5.2 Worker / Cluster 编程接口

用户通过 decorator 声明三类行为：

- `@register(mode="execute_all")`：单控制器风格，广播调用所有 worker，例如训练 worker 的 gradient computation。
- `@hw_mapping(hw_affinity={...})`：按 task domain 或 tag 把 generation 请求路由到 H800 / H20 等不同资源。例如 FrozenLake 路由 H800，default 路由 H20。
- `@register_serverless(...)`：把 reward method 替换成远程 serverless call，把固定 reward LLM 或 rule reward 当作 stateless function 调用。

这套接口的关键是保留 algorithm developer 的单控制器体验，同时把资源选择交给 runtime metadata 和 decorator declaration。

#### 5.3 轨迹级 rollout

每个 `EnvManager` 管一条环境轨迹，循环执行：

1. reset environment；
2. 把历史 observation/action 序列交给 `LLMProxy`；
3. `LLMProxy` 将请求发给 generation worker；
4. inference worker 在 command-driven loop 中处理 `ADD` / `ABORT`，无命令时继续推进 prefill/decode；
5. generation 完成后 callback 返回 action；
6. `EnvManager` 执行 env.step，记录 observation；
7. trajectory 完成后立即异步提交 reward。

这样慢环境只影响自己的 trajectory；快环境继续推进，reward 也可在单条轨迹完成后开始。

#### 5.4 Bounded-staleness async training

RollArt 的异步训练流程包含六步：

1. `get_batch`: training cluster 从 `SampleBuffer` 阻塞取一批 scored trajectories。
2. `suspend`: runtime 暂停 `LLMProxy` 接收新 generation request，保留 in-flight trajectories。
3. `update`: training workers 发布新权重，inference workers 更新。
4. `resume`: `LLMProxy` 恢复请求。
5. `recomp & rollout`: 对 in-flight trajectories 做 KV cache recomputation，让轨迹在新权重下继续。
6. `train_step`: training cluster 用第 1 步取到的 batch 更新模型，同时 rollout 继续产生新数据。

Staleness bound $\alpha$ 定义为：当前 agent LLM 是版本 $n$ 时，`SampleBuffer` 中 trajectory 的起始模型版本不得早于 $n-\alpha$。过期 trajectory 会被 abort / evict。这样 buffer size 受 $O(\alpha \cdot E)$ 约束，其中 $E$ 是并发环境数。

#### 5.5 跨集群数据移动与 PD disaggregation

RollArt 的最大通信负担来自权重同步。它用 Mooncake store 做异步 weight bucket 发布：

- training workers 将 updated weights 分桶写入远端 CPU-resident Mooncake store；
- inference workers 按需拉取最新 bucket；
- cross-cluster Ethernet 上的 push 和 inference-side pull 尽量与 rollout 重叠。

对于 prefill-decoding (PD) disaggregation，RollArt 支持将 prefill worker 放到 compute-optimized H800，将 decoding worker 放到 bandwidth-optimized H20，由 `LLMProxy` 按 phase 路由。

## 关键实验/定理

### 结果 1：Agentic RL step 的瓶颈来自多类 workload

- 设置：Qwen3-8B/32K，在 32 H800 GPUs 上跑 SWE-bench，batch size 128；比较 5 个 successful iterations 和 5 个包含 environment failures 的 iterations。
- Baseline：常规 synchronous step breakdown。
- 指标：step time breakdown；rollout / training / env.reset / overhead 占比。
- 结果：successful runs 平均 step time 365.7s，LLM generation 约 54%，training 约 23%，env initialization 约 15%；environment failure runs 平均 513.3s，env.reset 占 rollout time 78%；生产数据中 failure 约每 10 个 iterations 出现一次。
- 解读：agentic RL 的系统优化不能只处理 generation throughput，还要把 environment reset / step、training、reward 和跨集群传权纳入整体 pipeline。

### 结果 2：H800 / H20 在不同 rollout profile 下存在相反硬件亲和性

- 设置：Qwen3-8B/32K，prefix caching enabled；比较 prefill-heavy FrozenLake 与 decode-heavy GEM-Math；同成本硬件为 6 H20 vs 2 H800。
- Baseline：固定 rollout GPU class。
- 指标：end-to-end rollout time。
- 结果：FrozenLake 上 H800 将 rollout time 降到 H20 的最低 0.53 倍；GEM-Math 上 H20 将 rollout time 降到 H800 的 0.49 到 0.79 倍。
- 解读：generation stage 不能统一视为 bandwidth-bound。多轮任务会反复 prefill growing history，compute-optimized GPU 更合适；少轮长 CoT 任务更接近 decode-heavy，bandwidth-optimized GPU 更合适。

### 结果 3：End-to-end training time-to-score

- 设置：Qwen3-32B，32K max context，agentic task mix；RollArt 部署在 96 H800 + 32 H20，跨集群 200 Gbps Ethernet，集群内 400 Gbps InfiniBand；GRPO，batch size 512，group size 8；training 用 Megatron v0.12.2，rollout 用 vLLM 0.8.4；异步训练默认 $\alpha=1$。
- Baseline：Sync、Sync+、One-off、AReaL；Sync+ 加入 async reward、async environment interaction 和 serverless offloading；One-off 和 AReaL 为作者在 RollArt codebase 中复现/实现；Laminar closed-source，论文用 feature decomposition 讨论。
- 指标：target validation score 0.85 的 time-to-score；throughput；step time reduction。
- 结果：RollArt 相对 Sync+、One-off、AReaL 分别带来 2.05x、1.35x、1.31x step-time reduction；相对 Sync 吞吐提升 2.65 到 4.58 倍；相对 AReaL 的额外收益主要来自 hardware-affinity mapping 和更细 staleness control。
- 解读：系统收益来自多个 stage 的组合，尤其是异步 training/rollout overlap + hardware-affinity routing。由于 AReaL 是作者复现，结论应理解为同一代码库/同一硬件条件下的比较。

### 结果 4：四个设计点的 ablation

- 设置：分别隔离 R1/R2/R3/R4；模型包括 Qwen3-8B/14B/32B；R1 固定 32 H800 training，对比 72 H800 rollout、208 H20 rollout、64 H800 + 24 H20 mixed rollout；R2 用 synthetic env latency $\mu=10$s、$\sigma=1$ 到 10s；R3 使用三并发数学 agentic RL jobs，Qwen3-8B/16K actor，Qwen2.5-7B reward LLM；R4 扫 $\alpha=1$ 到 6。
- Baseline：homogeneous H800/H20 rollout；batch-level env interaction；local reward GPU；不同 staleness bound。
- 指标：step time、rollout time、GPU utilization、time-to-score。
- 结果：
  - R1: mixed hardware routing 相比 H20-only 降低 step time 1.30 到 1.68 倍，相比 H800-only 降低 1.12 到 1.37 倍。
  - R2: trajectory-level rollout 在 env latency variance 增大时比 batch-level 快 1.23 到 2.27 倍。
  - R3: serverless reward 将平均 GPU utilization 从 6% 提到 88%，平均 rollout time 从 158s 降到 77s。
  - R4: 更大的 $\alpha$ 最多相对 $\alpha=1$ 进一步改善 step time 1.22 倍，但 $\alpha=2$ 在后期 time-to-score 上已经出现回退，默认采用 $\alpha=1$。
- 解读：RollArt 的收益来自多个系统环节的组合，四个设计点分别对应硬件错配、环境长尾、reward 低利用和同步传权等待。$\alpha$ 的实验也提醒 async 越激进未必带来更好 time-to-score。

### 结果 5：跨集群通信和 PD disaggregation

- 设置：Qwen3-8B/14B/32B 权重同步；TCP 200 Gbps Ethernet 与 RDMA 400 Gbps InfiniBand；Mooncake v0.3.7；PD disaggregation 在 Qwen3-32B dense 与 Qwen3-30B-A3B MoE SWE task 上评测 1P3D / 2P2D。
- Baseline：veRL-style NCCL synchronous cross-cluster communication；PD colocation。
- 指标：weight transfer overhead、exposed pull cost、rollout time。
- 结果：
  - Qwen3-8B/14B/32B 权重大小 15.26GB / 27.51GB / 61.02GB；RDMA 相比 TCP speedup 分别 1.264x / 2.482x / 3.140x。
  - Naive push + accumulated pull no-overlap 成本为 38.6s / 84.1s / 157.0s；RollArt 暴露出来的 pull overhead 降至 1.4s / 5.1s / 9.6s。
  - Async cross-cluster communication 降低 step time 1.10 到 1.16 倍。
  - PD disaggregation 对 Qwen3-32B dense 提供 1.03 到 1.05 倍 modest speedup；对 Qwen3-30B-A3B MoE 1P3D / 2P2D 提供约 1.11x / 1.21x speedup。
- 解读：权重同步是 disaggregation tax 主体；Mooncake overlap 主要价值在隐藏 exposed cost。PD disaggregation 的收益依赖模型和 workload，MoE 下更明显。

### 结果 6：3000+ GPU 生产部署

- 设置：过去 9 个月数千个 agentic RL jobs 使用 RollArt；论文报告一个超过 3000 GPUs、持续一周、训练 hundreds-of-billions-parameter MoE model for Qoder product 的生产部署。
- Baseline：生产初始配置与调优后配置。
- 指标：prompt / response length、turn counts、step time distribution、GPU idle / get_batch 等待、env.reset success。
- 结果：prompt / response 分别可达 12k / 46k tokens；task 平均 turns 1 到 48；每步最大 response length 超过均值 5x，峰值 9x；最大 environment turns 超过均值 40x；最长 iteration 达 1.5 小时；`get_batch` 等待造成最高 62% iteration time 的 GPU idleness；资源比和 prefix cache 等调优后前 25 steps 端到端 speedup 1.66x；多级镜像 cache 将 env.reset success rate 提到 99.99% 以上，99.99% initialization 在 1 分钟内完成；一周训练只观察到一次 failure。
- 解读：生产段落是本文最强的生态证据，但缺少可复现 trace / raw logs。它说明 RollArt 解决的问题确实出现在 Alibaba 规模化 agentic RL，而非仅来自实验室构造。

### 实验设置与 baseline 审计

| 维度 | 记录 |
| --- | --- |
| 模型与初始化 | Qwen3 8B/14B/32B；32K max context；生产段落包含 hundreds-of-billions-parameter MoE model for Qoder product；MoE 具体结构未披露。 |
| 数据与任务 | SWE-bench、WebShop、FrozenLake、GEM-Math、GEM-Game；训练使用多任务 agentic mixture；数学任务使用 Qwen2.5-7B reward LLM 验证 reasoning process；具体 prompt 数、过滤规则、去重和数据污染检查未披露。 |
| RL / 训练配置 | GRPO (Group Relative Policy Optimization，组相对策略优化)；batch size 512；group size 8；uniform task sampling；asynchronous bound $\alpha=1$ 默认；$\alpha=1..6$ ablation。KL、entropy、learning rate、optimizer、训练步数未充分披露。 |
| 系统配置 | 96 H800 + 32 H20；集群内 400 Gbps InfiniBand，跨集群 200 Gbps Ethernet；CPU clusters for environments；internal serverless platform for reward workers；NCCL v2.26.5；Mooncake v0.3.7；vLLM 0.8.4 with prefix caching and CUDA graphs；Megatron v0.12.2。 |
| 技术报告训练配置 | 生产段落说明 3000+ GPUs、一周、1:5 training:generation GPU ratio、$\alpha=1$；模型、数据、并行细节、optimizer、token budget、cost 未完整披露。 |
| 训练硬件与拓扑 | Evaluation 为 H800/H20 disaggregated clusters；production 为 Alibaba cluster with above 3000 GPUs；具体节点数、每节点 GPU、NVLink / IB / Ethernet 拓扑只在 evaluation 披露到 400 Gbps IB / 200 Gbps Ethernet。 |
| 并行方式与框架 | Rollout TP: Qwen3-8B/14B/32B 为 1/2/4；training-side TP/PP tuned for throughput，具体度数未披露；Megatron trainer，vLLM rollout，Ray control/data plane，Kubernetes env，serverless reward。 |
| 训练数据规模与组成 | 未披露；只披露任务域和 production prompt/response length/turn range。 |
| 训练过程与超参 | GRPO batch/group size 披露；learning rate、clip、KL、optimizer、warmup、gradient clipping、checkpoint policy 未披露。 |
| 训练时间 / GPU hours / 成本 | Production 一周、3000+ GPUs；evaluation 说明 RollArt per-GPU-hour cost 约为 baselines 的 83%；总 GPU hours / 成本未披露。 |
| 未披露项 | 任务数据集规模、reward 细节、exact model initialization、optimizer/lr、KL/entropy、seed、误差线、AReaL 复现细节、Laminar 直接对比、开源 artifact mapping。 |
| 评测协议 | End-to-end latency 为 5 iterations average step time；throughput 为 global batch prompt/response tokens divided by step time；validation score 每 10 iterations 评估；target score 0.85；部分 ablation 使用 10 iterations。 |
| 统计报告 | 多数图为平均值，无 seed、confidence interval、显著性检验；production trace 是单个大规模案例。 |
| Baseline 是否 tuned | Sync+ 被作者增强，One-off/AReaL 复现包含 Sync+ optimizations；训练侧 parallelism tuned for throughput；baseline 调参程度未完整披露。 |
| Baseline 是否 compute-matched | Baselines on 128 H800，RollArt uses 96 H800 + 32 H20 and per-GPU-hour cost约 83%；作者强调同等或更低成本，但不同 GPU type 的成本模型依赖 megascale-infer citation。 |
| Baseline 是否 implementation-matched | Sync/Sync+/One-off/AReaL 在 RollArt codebase 中实现或复现，implementation-matched 较强；Laminar closed-source，无直接实现。 |
| Baseline 是否覆盖强替代方案 | 覆盖 veRL-style sync、One-off、AReaL、Laminar feature decomposition；未覆盖所有 production frameworks 或 slime/GLM-5 internal pipeline 的直接对比。 |
| Baseline 是否存在弱化风险 | AReaL 复现与原实现可能不同；Laminar 仅做能力缺失分解；serverless reward 平台是内部能力，外部系统未必可用。 |
| 结论边界 | 结论可表述为：在异构 GPU + CPU/Kubernetes + serverless + Mooncake 等条件下，RollArt 相对同代码库 baseline 显著提升 agentic RL step time 和 throughput；对任意同构集群或严格 on-policy setting 的收益需重新验证。 |

## 证据链强度评估

### 强证据

- Workload characterization 覆盖 generation、environment、reward、weight sync 四类瓶颈，并给出 SWE-bench step breakdown、H800/H20 亲和性、reward GPU utilization、weight transfer overhead。
- End-to-end evaluation 和 ablation 能把四个设计点分别对应到可测收益，尤其 R1/R2/R3/R4 的隔离实验清晰。
- USENIX OSDI 2026 presentation page、ROLL public repo 和 Wei Gao / Yuheng Zhao / Wei Wang 页面共同证明 RollArt 属于 HKUST-Alibaba / ROLL 线的真实系统项目。
- 生产段落提供了 3000+ GPU、一周训练、environment failure mitigation、get_batch bottleneck 等 production scale 证据。

### 中等强度证据

- AReaL / One-off 比较在同一代码库内复现，implementation consistency 较好，但仍需要原系统作者或第三方复验。
- PD disaggregation 对 dense / MoE 的收益方向合理，但 dense 只有 1.03 到 1.05 倍，结论应限定为 workload/model dependent。
- $\alpha=1$ 的 time-to-score 结论与图中趋势一致，但缺少 seed / confidence interval。

### 需要谨慎的推论

- “优于 Laminar”主要来自 feature decomposition，缺少直接运行 Laminar 的 apples-to-apples 数字。
- Serverless reward 的收益依赖内部 serverless 平台、reward function statelessness、网络 I/O 和弹性资源成本。
- 生产可扩展性强，但 raw trace、failure log、具体 MoE model / data / reward 没有公开。
- 静态 domain-level hardware mapping 在本文生产 workload 中足够稳定；对动态变化更强的 agent domains，需要在线 profiler 或自适应调度。

## OpenReview / 审稿意见吸收

- Venue status: USENIX OSDI 2026 presentation page已公开列出 RollArt；TeX acknowledges shepherd and anonymous reviewers。观察日期：2026-06-30。
- Public reviews: 未发现公开 reviewer comments / OpenReview / HotCRP reviews。
- Ratings / confidence: 未公开。
- Reviewer consensus: 未公开；可从 v2 TeX 中 shepherd edits / acknowledgment 推断论文经过 USENIX review pipeline，但不能据此还原 reviewer 具体意见。
- Main criticisms: 未公开。
- Author response: 未公开。
- 对本文可信度的影响: OSDI 2026 页面提升 venue-status 可信度；技术结论仍需按公开论文实验和 artifact 可复验程度评估。公开 review 缺失意味着 baseline fairness、统计显著性和 artifact 完整性仍需后续复查。

## 本地讨论补充

### 1. 讨论收敛点

- 这篇论文此前没有本地条目，因此用户在归档站点里看不到 RollArt；本次新增独立 Markdown 和索引入口。
- RollArt 应归入 RLHF systems / distributed post-training infrastructure cluster，同时和 agentic RL / self-improving agents umbrella 相连。

### 2. 修正后的理解

- RollArt 的重点是 agentic RL pipeline 的 stage/sub-stage 粒度异构性，并引入 resource plane / data plane / control plane 三层来承载多资源池。
- 它和 Laminar 的主要差异：Laminar 更强调 trajectory-level async post-training 和 relay workers；RollArt 进一步加入 H800/H20 task/PD affinity、CPU environment、serverless reward、Mooncake async weight movement 和 3000+ GPU production trace。
- 它和 slime / GLM-5 的关系：slime/GLM-5 说明长轨迹 agentic RL 需要 async rollout、TITO、direct IS 和生产基础设施；RollArt 从 Alibaba / ROLL 角度给出 disaggregated resource fabric 的系统方案。

### 3. 后续复验指标

- 检查 ROLL repo 是否发布 RollArt artifact、配置文件、paper-specific examples 和 reproducibility scripts。
- 对比 ROLL async training feature、AReaL 原实现、verl fully async、Laminar closed-source paper 参数，验证 baseline fairness。
- 记录每个 task domain 的 prefill tokens、decode tokens、turn count、env reset/step latency、reward latency 和 staleness distribution，评估 static mapping 是否足够。
- 在同构集群和无 serverless 平台环境中测 RollArt 各组件 residual gain，区分异构资源收益和纯 runtime 收益。

## 主要启发

- Agentic RL 的系统瓶颈应该按 stage 和 sub-stage 建模：prefill、decode、environment、reward、training、weight sync 的硬件亲和性和等待模式不同。
- Trajectory-level scheduling 是 agentic RL runtime 的基础能力。只要 environment 有重尾，batch barrier 就会把少数慢轨迹的延迟扩散到整批样本。
- Serverless reward 是很实用的设计点，但前提是 reward 可以无状态化，且远程 I/O 成本小于空置 GPU 成本。
- Bounded staleness 要同时看 step time 和 time-to-score。更大 $\alpha$ 会降低等待，但也会改变样本新鲜度和收敛质量。
- 对后续读 agentic RL 系统论文，应统一记录：task turn distribution、prefill/decode ratio、environment failure rate、reward worker utilization、weight sync exposed cost、sample staleness histogram。

## 局限

1. 静态 hardware-affinity mapping 依赖用户或运维者对 task domain 的先验认识；当 workload profile 在训练中显著变化时，系统需要在线 profiler 和自适应 mapping。
2. Baseline fairness 仍需第三方复验，尤其 AReaL 复现、Laminar feature decomposition 和不同系统对 serverless reward / async env 的支持差异。
3. 训练超参、数据规模、reward 细节、seed 和统计显著性披露不足，模型质量结论主要依赖 time-to-score 曲线。
4. Production deployment 细节较少，缺少 raw trace、故障日志、MoE 架构、数据、cost 和 GPU-hour 拆分。
5. Serverless reward、H800/H20 成本模型、Mooncake weight store 和内部 Kubernetes/cache 机制具有平台依赖性，迁移到其他云或同构集群时收益可能下降。
6. 严格 on-policy 训练或对 staleness 极敏感的 reward/objective，可能无法直接采用 $\alpha=1$ async training。

## 跨论文关系

- 与 [HybridFlow / VERL](/papers/2409.19256-hybridflow-rlhf-framework/)：HybridFlow 提供 RLHF/RLVR dataflow 编排和 hybrid controller；RollArt 承接单控制器体验，但把资源 plane 扩展到 H800/H20/CPU/serverless 的 heterogeneity-aware binding。
- 与 [Laminar](/papers/2510.12633-laminar-asynchronous-rl-post-training/)：两者都关注 asynchronous RL post-training 和 trajectory-level asynchrony。Laminar 更强调 relay workers、RDMA weight sync、dynamic repack、experience buffer sampling；RollArt 更强调 multi-task agentic RL 的资源异构、serverless reward 和 production task-domain affinity。
- 与 [slime](/papers/2026-06-17-slime-rl-scaling-framework/) / [GLM-5](/papers/2602.15763-glm-5-agentic-engineering/)：slime / GLM-5 代表 THUDM / Z.ai 的 async agentic RL infrastructure，侧重 train-rollout consistency、TITO、direct double-sided IS、DP-aware routing；RollArt 代表 Alibaba / HKUST / ROLL 的 disaggregated resource fabric，侧重跨硬件资源池映射和生产调度。
- 与 [Seer](/papers/2511.14617-seer-online-context-learning-llm-rl/)：Seer 关注 synchronous RL rollout 中的 context scheduling、global KVCache、grouped speculative decoding；RollArt 关注 agentic RL 多 stage disaggregation。二者都服务 rollout bottleneck，但优化层次不同。
- 与 [Bebop](/papers/2606.12370-bebop-mtp-rejection-sampling-rl-training/)：Bebop 优化单条 decode / MTP acceptance，RollArt 优化 stage/sub-stage placement 和 rollout/training overlap；Bebop 可作为 RollArt rollout engine 内的 decode acceleration 模块。
- 与 [Kimi K2/K2.5](/papers/2507.20534-kimi-k2-open-agentic-intelligence/)：Kimi 系列展示 agentic intelligence 的模型与训练 pipeline，RollArt 提供另一条 production training infrastructure 证据；Mooncake 在 RollArt 中作为 weight movement / store 组件被引用。

## Reference Intake Brief

### Target

- Intended target system: 新增论文笔记 / 论文索引 / 作者档案。
- Existing related assets: `papers-index.md`；[HybridFlow / VERL](/papers/2409.19256-hybridflow-rlhf-framework/)、[Laminar](/papers/2510.12633-laminar-asynchronous-rl-post-training/)、[slime](/papers/2026-06-17-slime-rl-scaling-framework/)、[Seer](/papers/2511.14617-seer-online-context-learning-llm-rl/)、[Bebop](/papers/2606.12370-bebop-mtp-rejection-sampling-rl-training/)、[GLM-5](/papers/2602.15763-glm-5-agentic-engineering/)。
- Proposed form: 新建独立 Markdown 文档，并更新 `papers-index.md` 与 `authors.json`。

### Reusable Elements

1. 四类异构 workload 的 profiling checklist：generation prefill/decode、environment、reward、training/weight sync。
2. Bounded-staleness async training 的六步协议和 $\alpha$ 诊断语言。
3. Agentic RL systems baseline audit：Sync/Sync+/One-off/AReaL/Laminar/slime/verl 的能力拆分。
4. Production trace 字段：turn distribution、response length tail、get_batch waiting、env.reset success、image cache、failure recovery。

### Risks

- Copyright/over-copying: 已用概括性转写，少量数值来自论文公开表格/正文；避免大段复制。
- Unsourced or unverifiable claims: Production-scale、serverless 平台、MoE details 主要来自作者报告，已在局限中标注。
- Tone/brand mismatch: 中文分析保持工程审计语气，避免把 OSDI status 当成充分证明。
- Safety/compliance issues: 无直接安全攻击流程；涉及 agentic RL 仅记录系统训练与环境调度机制。
- Overlap with existing assets: 与 Laminar / slime / verl / Seer / Bebop 有强 overlap，已通过跨论文关系区分。

### Skipped

| Material | Reason |
| --- | --- |
| 公开 reviewer comments | 未发现 OpenReview / HotCRP / USENIX 公开审稿文本；USENIX 页面只公开 presentation metadata 和摘要。 |
| 完整 artifact mapping | ROLL 仓库公开，但本文 v2 未显式给出 RollArt 模块和复现实验配置映射；需后续随 OSDI artifact 检查。 |
| Production raw trace | 论文只公开聚合统计和图表，无原始 trace / logs。 |

### Recommendation

Decision: merge

Why: RollArt 是当前档案中 agentic RL infrastructure 的关键缺口。已有 Laminar / slime / verl / Seer / Bebop 分别覆盖 async post-training、RL scaling framework、rollout optimization、MTP decode acceleration；RollArt 补上 Alibaba / HKUST / ROLL 线的异构资源池、serverless reward、trajectory-level rollout 和 3000+ GPU production deployment 证据。
